# Auditoria QA E2E — viagem → reconciliação (2026-06-12)

Escopo: pipeline `viagem → baplie → manifesto → veículos → taxas → faturamento → reconciliação`.
Método: leitura completa de docs/rotas/migrations, fixtures importáveis validadas contra os parsers reais
(`test-fixtures/`), suíte de testes (381 passed / 9 skipped) e auditoria de código profunda das três
fases do pipeline. Execução E2E no browser: ver seção "Execução ao vivo".

---

## 1. Broken / Not Working

### Importação (viagem → baplie → manifesto → veículos)

| # | Tela / arquivo | Problema | Causa suspeita |
|---|---|---|---|
| B1 | Baplie — `src/services/baplieImport.ts:10-43` | Reimport apaga staging anterior antes do insert; se um batch de insert falhar, a viagem fica com staging vazio/parcial, sem rollback. | delete+insert não transacional. |
| B2 | Baplie — `src/services/baplieReconciliation.ts:80,91` | Chave de match do manifesto remove espaços (`replace(/\s+/g,'')`), a do Baplie não — container staged com espaço interno vira falso `missing_in_manifest`. | Normalização assimétrica da match key. |
| B3 | Baplie — `src/pages/Baplie.tsx:91-95,118` | Ações de vazios não invalidam `baplie-staging`/`baplie-reconciliation`; painéis de divergência podem mostrar dados velhos. | Invalidation incompleta. |
| B4 | Manifestos — `src/services/manifestImport.ts:152-172` | RPC transacional commita, mas sync de schedules e `run_billing_for_import_batch` rodam depois; se falharem, a UI reporta erro para um import que persistiu. | Pós-processamento não atômico. |
| B5 | Manifestos — `src/services/manifestImport.ts:177-221` | Selects/updates de pós-billing ignoram `error` do Supabase — falha silenciosa deixa B/Ls sem flags de revisão/hold. | Erros engolidos. |
| B6 | Veículos — `src/services/vehicleImport.ts:324-363` | Insert em chunks sem rollback: falha num chunk posterior lança erro genérico com linhas anteriores já persistidas. | Import parcial sem compensação. |
| B7 | Veículos — `src/components/shared/VoyageImportActions.tsx:474-478` e `src/pages/Veiculos.tsx:137-140` | Import de veículos não invalida `vehicle-stats`/`voyage-vehicle-stats`; cards de resumo ficam desatualizados. | Invalidation incompleta. |

### Taxas / Faturamento

| # | Tela / arquivo | Problema | Causa suspeita |
|---|---|---|---|
| B8 | Faturamento — `src/services/billing.ts:700-704` | `createInvoiceFromBls`: invoice é emitida pela RPC e o `link_invoice_to_ledger` roda depois; se o link falhar, fica invoice `issued` órfã do ledger, sem rollback. | Emissão + link não atômicos. |
| B9 | Revisão/B/L — `src/services/reviewBillingAutomation.ts:36-44`, `src/components/bl/BlCobrancasTab.tsx:162-164` | `markBlReadyForBilling` ok + falha na invoice → B/L preso em `ready_for_billing` sem fatura; mensagem diz "bloqueado" mas o estado já avançou. | Máquina de estados sem compensação. |
| B10 | B/L Cobranças — `BlCobrancasTab.tsx:164-166` | Após emitir fatura manual só invalida `['invoices']`; Validação/Pendências seguem mostrando o B/L como "Pronto". | Invalidation incompleta. |
| B11 | Faturamento (granito) — `chargeOperationsService.ts:431-435` | Moeda desconhecida/nula é somada como BRL no subtotal. | Bucketing `USD` vs "resto = BRL". |
| B12 | Faturamento Validação — `ValidacaoTab.tsx:80,90,509,708-714` | `matched_name` é exibido como badge de match mas contado como "Conciliação pendente" — bloqueio invisível para o operador. | Definições de "resolvido" divergentes entre contador e badge. |

### Reconciliação

| # | Tela / arquivo | Problema | Causa suspeita |
|---|---|---|---|
| B13 | Conciliação PIX — `src/services/reconciliacao.ts:53-66` | `txidMap` único para invoices locais + demurrage; colisão de número de documento sobrescreve silenciosamente → pagamento pode conciliar contra o documento errado. | Mapa sem detecção de colisão. |
| B14 | Conciliação PIX — `reconciliacao.ts:68-78`, `Reconciliacao.tsx:55,168-195` | `ambiguous` é hardcoded `false`; todo o UI de "Ambíguas — ignoradas" é código morto e dá falsa sensação de proteção. | Detecção de ambiguidade nunca implementada no client. |
| B15 | Conciliação PIX — `Reconciliacao.tsx:43-53`, `reconciliacao.ts:86-99` | Loop de settlement sem transação: falha no item N deixa 1..N-1 pagos no banco, mas a UI mostra só erro, sem invalidation nem resumo parcial. | Batch não atômico + onSuccess nunca roda. |
| B16 | Conciliação PIX — `reconciliacao.ts:62-65,101-105` | Demurrage com valor divergente aparece como match verde "sem conflito" e só estoura no confirm — abortando o restante do batch (B15). | Sem comparação de valor no match. |
| B17 | Conciliação PIX — `Reconciliacao.tsx:45-51` | Confirm invalida só `demurrage-invoices`/`invoices`/`demurrage-kpis`; `bls.financial_status='paid'` e a ficha do cliente (`customer-detail`) ficam stale até refresh manual. | Invalidation incompleta (status final não propaga). |

## 2. Poor Usability

| # | Tela | Problema | Correção sugerida |
|---|---|---|---|
| U1 | Taxas Locais | A página não tem nenhuma ação de cálculo (só Tabelas/Overrides), mas a Revisão manda o operador para lá ("Calcular em lote") — beco sem saída. | Apontar o link para Faturamento → Validação ou adicionar ação de cálculo na página. |
| U2 | Faturamento | Não existe botão direto "emitir fatura individual"; emissão só acontece via automações (Revisão, lote da Validação, aba Cobranças do B/L). | Adicionar emissão explícita por B/L pronto na aba Faturas/Validação. |
| U3 | Validação | Não há um lugar único que explique por que um B/L não pode ser faturado (cliente/CE/peso/conciliação espalhados em badges e contadores). | Consolidar os motivos de bloqueio numa coluna/painel "Por que não fatura?". |
| U4 | Conciliação PIX | Badge fixo "Valor e documento sem conflito" em toda linha, mesmo sem checagem de valor (demurrage). | Só exibir o badge após comparação real de valor. |
| U5 | Conciliação PIX | TXID truncado (`max-w-[180px]`) sem tooltip — operador não consegue auditar o campo mais importante. | Adicionar `title`/tooltip com o TXID completo. |
| U6 | Conciliação PIX | Confirmação em lote sem progresso nem resultado por linha. | Reportar resultado por item (ok/falha) ao final. |
| U7 | Manifestos | Reabrir o modal de import mostra a seleção/preview do arquivo anterior (estado não resetado). | Resetar `files`/preview ao fechar o modal. |
| U8 | Manifestos/Veículos/Baplie | Com exatamente 1 viagem cadastrada o select re-seleciona sozinho e fica impossível de limpar. | Auto-selecionar só na montagem. |
| U9 | Veículos | Com erros no relatório de import, "Confirmar importação" continua habilitado e re-submete as mesmas linhas (falham como "Chassi já cadastrado"). | Desabilitar re-submit após processamento. |
| U10 | Veículos | Tile "Viagem selecionada" mostra `1`/`0` como KPI. | Mostrar o nome da viagem ou um check. |
| U11 | Baplie | Durante o load do manifesto de vazios o botão "Confirmar cadastro" aparece e clicar não faz nada (guard silencioso). | Mostrar loading e desabilitar a ação até resolver. |

## 3. Warnings / Minor

- `ChargeTablesTab.tsx:573` — Fragment de map sem `key` (warning de React, possível glitch em expand/collapse).
- `Faturamento.tsx:102-110` — `detectOverdueInvoices` fire-and-forget sem `.catch` (unhandled rejection no console).
- `VoyageImportActions.tsx:217` / `Manifestos.tsx:590` — falha no hash do arquivo desliga silenciosamente o dedupe (`fileHash: null`), permitindo importar o mesmo arquivo duas vezes.
- `manifestParser.ts:165-171` — duplicata de container no mesmo B/L não é detectada se intercalada por linhas de outro B/L (mapa guarda só o último).
- `reconciliacao.ts:88` — `paid_at` cai para a data do browser quando o parse da data do extrato falha.
- `baplieReconciliation.ts:104-135` — container Part Lot (em N B/Ls) gera N divergências para 1 container físico; "Aceitar Baplie" em massa pode sobrescrever splits de outro B/L.
- `Baplie.tsx:419` — contador de PODs é derivado aritmeticamente e pode ficar negativo/errado após re-parse.
- `InvoiceDetailModal` — pagamento parcial não existe na UI de ledger (campo readonly com saldo cheio) embora `partially_paid` seja status aceito.
- `FinancialAlertsPanel` — sem skeleton durante o load (render com `alerts ?? []`).

## 4. Fixtures de teste

`test-fixtures/` (validadas contra os parsers reais via Vitest — 3/3 passed):
- `qa-baplie-3cntr.edi` — 3 containers (1 IMO classe 9/UN3171), 2 B/Ls, viagem `QA001E`.
- `qa-manifest-cntr.xlsx` — 2 B/Ls / 3 containers, CNPJs válidos, POD BRSSA (coberto pela tabela "Salvador CNTR 2026").
- `qa-veiculos.xlsx` — 2 veículos no `TEMU1234567` / `QABL001`.
- Extrato PIX Itaú: gerado durante a execução ao vivo (TXID = número da invoice emitida).

## 5. Execução ao vivo (browser)

> **Nota:** 5.1–5.5 registram a 1ª tentativa (PR #212), **bloqueada no login por egress**.
> **5.6 registra a 2ª passagem (egress liberado) com o fluxo completo 1–9 executado.**

**Status: PARCIAL.** Tentativa de execução ao vivo em 2026-06-12 (Fase 3). Desta vez o app
subiu e foi exercitado no browser real (Playwright/Chromium), mas o fluxo trava no **Passo 1
(login)** porque a política de egress do container continua bloqueando `*.supabase.co`. Como
todos os Passos 2–9 dependem do app falar com o Supabase (Auth + PostgREST), nenhum deles pôde
ser executado.

### 5.1 O que rodou nesta sessão (evidência)

| Item | Resultado |
|---|---|
| `.env` preenchido via MCP (`get_project_url` / `get_publishable_keys`, projeto `fgmkhbzhaeebrsizwccx`) | OK |
| Senha do usuário de QA redefinida via MCP SQL (`qa.audit@transhipping.test` → `QaTest2026!live`) | OK (id `1b0bfc3b-…`) |
| `npm install --legacy-peer-deps` + `npx vite --port 5173` | OK — app servido em `http://localhost:5173` |
| Browser carrega `/login`, formulário renderiza | OK |
| **POST `https://fgmkhbzhaeebrsizwccx.supabase.co/auth/v1/token?grant_type=password`** | **`net::ERR_FAILED`** |

Egress confirmado bloqueado fora do browser também: `curl` a `*.supabase.co` retorna
`403 — "Host not in allowlist: fgmkhbzhaeebrsizwccx.supabase.co"`. `github.com`/`registry.npmjs.org`
respondem `200`. Docker indisponível no host (`docker info` falha), então o stack local também
não é uma alternativa nesta sessão. A política de egress é aplicada na criação do container e não
é editável retroativamente. **Os Passos 2–9 exigem uma nova sessão com `*.supabase.co` no allowlist
de egress.**

### 5.2 Erros de console capturados no Passo 1 (login)

```
[ERROR] Access to fetch at 'https://fgmkhbzhaeebrsizwccx.supabase.co/auth/v1/token?grant_type=password'
        from origin 'http://localhost:5173' has been blocked by CORS policy: Response to preflight
        request doesn't pass access control check: No 'Access-Control-Allow-Origin' header is present.
[ERROR] Failed to load resource: net::ERR_FAILED  (.../auth/v1/token?grant_type=password)
[ERROR] TypeError: Failed to fetch
        at SupabaseAuthClient.signInWithPassword (@supabase/supabase-js)
        at signIn (src/hooks/useAuth.tsx:138)
        at handleSubmit (src/pages/Login.tsx:38)
```

O 403 do proxy de egress não carrega cabeçalhos CORS, então o browser reporta a falha como
violação de preflight CORS + `ERR_FAILED`. A causa-raiz é egress, não CORS do Supabase.

### 5.3 Novo achado de runtime (não identificado na auditoria de código)

| # | Tela / arquivo | Problema | Evidência |
|---|---|---|---|
| **R1** | Login — `src/pages/Login.tsx:32-39` | `handleSubmit` envolve `signIn` num `try { … } catch {}` que descarta o erro e renderiza **sempre** a mesma mensagem: *"Credenciais inválidas ou usuário sem permissão ativa."* Uma falha de **rede/transporte** (aqui, `ERR_FAILED` no token endpoint) é apresentada ao operador como credencial inválida — diagnóstico enganoso que mascara indisponibilidade do backend. | Observado ao vivo: token POST falhou por egress, UI exibiu "Credenciais inválidas…". Sugestão: distinguir erro de transporte (ex.: `TypeError`/`Failed to fetch`) de `AuthApiError` 400 e mostrar mensagem de indisponibilidade. |

### 5.4 Status dos bugs documentados (B1–B17, U1–U11) após esta sessão

Nenhum dos bugs B1–B17 / U1–U11 pôde ser **confirmado ou refutado ao vivo** — todos vivem em
telas posteriores ao login (baplie, manifestos, veículos, faturamento, reconciliação) que não
foram alcançadas. Permanecem como achados de auditoria de código (Seções 1–2), ainda não
verificados em runtime. A única observação de runtime adicionada por esta sessão é **R1** (acima),
na própria tela de login.

### 5.5 Playbook para a nova sessão (egress liberado)

1. `cp .env.example .env` e preencher `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` via MCP
   Supabase (`get_project_url` / `get_publishable_keys`, projeto `fgmkhbzhaeebrsizwccx`).
2. Login interno: usuário de QA já provisionado — `qa.audit@transhipping.test`
   (perfil `admin`, full_name "QA Audit (temporario)"). Redefinir a senha via SQL:
   `update auth.users set encrypted_password = crypt('<nova-senha>', gen_salt('bf')) where email = 'qa.audit@transhipping.test';`
3. `npm install --legacy-peer-deps && npx vite` e seguir com Playwright:
   - `/viagens` → **Nova Viagem** (Armador/SCAC/Navio/IMO/Número `QA001E`/Status Ativa).
   - `/baplie` → selecionar a viagem → **Importar Baplie EDI** → `test-fixtures/qa-baplie-3cntr.edi` → conferir 3 containers staged.
   - `/manifestos` → **Importar Manifesto CNTR** → `test-fixtures/qa-manifest-cntr.xlsx` → conferir 2 B/Ls / 3 containers e a conciliação Baplie×Manifesto em `/baplie`.
   - `/veiculos` → **Importar Veiculos** → `test-fixtures/qa-veiculos.xlsx` → conferir 2 veículos no `TEMU1234567`.
   - `/containers` e `/veiculos` → paginação/scroll/overflow.
   - Taxas: Faturamento → **Validação** → selecionar os B/Ls → **Recalcular selecionados** (tabela "Salvador CNTR 2026" cobre POD BRSSA). Não há ação de cálculo em `/taxas-locais` (ver U1).
   - Emissão: Revisão → vincular cliente (auto-emite) ou B/L → Cobranças → **Pronto para faturar**; conferir linhas e impressão (window.print) no `InvoiceDetailModal`.
   - Reconciliação: gerar extrato Itaú xlsx com header `identificador` + colunas `CPF/CNPJ`, `pago em` (dd/mm/aaaa), `valor pago` (formato BR), TXID = número da invoice emitida → `/reconciliacao` → upload → **Confirmar** → verificar propagação de status (atenção a B15/B17: refresh manual pode ser necessário).
4. Limpeza: remover viagem QA, B/Ls `QABL001/QABL002`, clientes de teste, invoices e o usuário
   `qa.audit@transhipping.test` (`docs/RESET_AMBIENTE.md`).

### 5.6 Execução ao vivo Fase 3 — fluxo completo 1–9 (egress liberado)

**Status: COMPLETO.** Segunda passagem em 2026-06-12 com `*.supabase.co` no allowlist. App
servido em `http://localhost:5173` (Vite 8), navegado via Playwright/Chromium. Todos os Passos
1–9 foram executados ao vivo contra o projeto `fgmkhbzhaeebrsizwccx`. Egress validado antes de
tudo: `GET /auth/v1/health` → `401` (alcançável). Usuário `qa.audit@transhipping.test`
(perfil Administrativo, "QA Audit (temporario)") com senha redefinida via MCP SQL.

#### O que funcionou conforme esperado

| Passo | Resultado |
|---|---|
| 1. Login | Credenciais válidas → redirect para `/painel`. Sem erros de app no console (só PTAX, externo). |
| 2. Nova Viagem | `QA TEST VESSEL / QA001E` criada (Armador QA SHIPPING LINE, SCAC QASC, IMO 9999999, POD BRSSA ETA 2026-07-15), aparece na lista. |
| 3. Baplie | `qa-baplie-3cntr.edi` importado: 3 containers staged, badge IMO em `CSNU2049996`, trecho CNSHA→BRSSA. Sem divergências de estrutura. |
| 4. Manifesto | `qa-manifest-cntr.xlsx`: preview 2 B/Ls (QABL001=2 cntr, QABL002=1 cntr), ambos OK. Conciliação Baplie×Manifesto = 3 containers; 1 divergência de atributo IMO (Classe 9 / UN 3171 presente no Baplie, ausente no manifesto) resolvida via "Aplicar Baplie a todos IMO" → 0 divergências. |
| 5. Veículos | `qa-veiculos.xlsx`: preview 2 linhas válidas / 0 erros. Persistidos VW NIVUS + PEUGEOT 2008 em `TEMU1234567` / `QABL001` / 40HC (lista mostra 2 veículos, peso total 2.610 kg). |
| 6. Listagens | `/containers`: os 3 containers presentes, badge IMO em `CSNU2049996`; `/veiculos`: 2 veículos, paginação 1/1. |
| 7. Taxas | `Recalcular selecionados` aplicou **"Salvador CNTR 2026"** ao QABL002: THD(imo) 2.575,50 + ISPS 50 + B/L Fee 600 + Drop Off 150 + Damage Protection 185 = **R$ 3.560,50** (5 linhas `auto` em `charge_calculations`, todas `status=calculated`). QABL001 é `exempt` por design (LCL/veículos, "taxas pagas na origem"). |
| 8. Emissão | Revisão de QABL002: cliente correto **QA COMERCIO EXTERIOR SA** (CNPJ 60746948000112) cadastrado e vinculado → `charge_status=ready_for_billing`. B/L → Cobranças → **Pronto para faturar** → **INV-2026-0041** emitida (`issued`, R$ 3.560,50). Modal "Imprimir" renderiza a "FATURA DE TAXAS LOCAIS" corretamente (logo, cliente/CNPJ, B/L, navio/viagem, itens). |
| 9. Conciliação | Extrato PIX Itaú gerado (Node+xlsx, header `identificador`, TXID=INV-2026-0041, valor `3.560,50`, CNPJ 60746948000112) → `/reconciliacao` → 1 correspondência única por TXID → **Confirmar**. Propagou: invoice `paid`, `bls.financial_status='paid'` (QABL002), `bl_receivables` `status=settled` / `balance_brl=0.00`. |

#### Novos bugs de runtime (não identificados na auditoria de código)

| # | Tela / camada | Problema | Evidência |
|---|---|---|---|
| **R2** | Faturamento → Validação (`Recalcular selecionados`) | Após o recálculo, a grade de Validação **não atualiza** — continua exibindo `Linhas: 0`, `Subtotal R$ 0,00` e o bloqueio antigo *"Falha crítica: Nenhuma tabela de preço…"*, mesmo com as 5 linhas já gravadas em `charge_calculations` (R$ 3.560,50). Só após reload manual o subtotal aparece. A resposta do recálculo não invalida/atualiza a query da grade. | DB: 5 linhas `calculated` às 13:11:40; UI manteve 0 linhas até F5. |
| **R3** | `bls.billing_hold_reason` no recálculo | O `Recalcular selecionados` grava as taxas mas **não limpa** `billing_hold_reason` herdado de um billing run anterior. O B/L permanece exibindo o bloqueio de "Nenhuma tabela de preço" mesmo com `charge_status='calculated'`. O reason só é limpo pelo fluxo de **revisão/reconciliação** (`Marcar como revisado` → `billing_hold_reason=NULL`). | DB QABL002: `charge_status=calculated`, `charges_calculated_at` atualizado, porém `billing_hold_reason` ainda setado até a revisão. |
| **R4** | Reconciliação de cliente por nome | Manifesto trouxe consignatário **"QA COMERCIO EXTERIOR SA"** (CNPJ 60746948000112). Sem cliente com esse CNPJ exato, o sistema sugeriu por **match de nome** um cliente diferente — **"AJ COMERCIO EXTERIOR LTDA"** (`customer_reconciliation_status=matched_name`, customer_id 10) — falso-positivo entre empresas distintas (só compartilham o token "COMERCIO EXTERIOR"). Corrigível em /revisão, mas o match heurístico por nome pode vincular o cliente errado se aprovado às cegas. | Painel de conciliação em Validação e /revisão; corrigido cadastrando o cliente correto (id 331). |

#### Confirmações / refutações dos bugs documentados

| Bug | Veredito ao vivo | Nota |
|---|---|---|
| **R1** (Login mascara falha de rede como credencial inválida) | **CONFIRMADO** | Senha errada → `POST /auth/v1/token` `400` → UI exibe "Credenciais inválidas ou usuário sem permissão ativa." O `catch {}` em `Login.tsx:35-36` trata rede e senha de forma idêntica; uma indisponibilidade de backend apareceria com a mesma mensagem. |
| **U1** (Taxas Locais sem ação de cálculo) | **CONFIRMADO** | `/taxas-locais` só tem abas Tabelas/Overrides e CRUD de tabela/item — nenhum botão de calcular. O cálculo vive em Faturamento → Validação (`Recalcular selecionados`) e no B/L → Cobranças. |
| **B17** (status final de pagamento não propaga sem refresh manual) | **NÃO REPRODUZIDO** | Após Confirmar a conciliação, invoice→`paid`, `bls.financial_status='paid'` e `bl_receivables.status='settled'`/`balance 0,00` propagaram corretamente no banco; o modal da fatura reabriu já como **Paga / Saldo R$ 0,00**. (A invalidação de `customer-detail` em tempo real não foi isolada, pois cada navegação recarrega os dados.) |
| Demais **B1–B16, U2–U11** | **NÃO EXERCITADOS** nesta passagem | Vivem em caminhos/edge-cases (BB/Granito, vazios, demurrage, overrides, paginação específica) fora do fluxo container 1–9 executado. Permanecem como achados de auditoria de código. |

#### Erros de console capturados (Fase 3 ao vivo)

Em todas as telas, os **únicos** erros recorrentes são externos e de infraestrutura, não da aplicação:

```
[ERROR] Access to fetch at 'https://olinda.bcb.gov.br/.../CotacaoDolarDia...'
        blocked by CORS policy: No 'Access-Control-Allow-Origin' header   (PTAX/BCB)
[ERROR] Failed to load resource: net::ERR_FAILED   (olinda.bcb.gov.br — fora do allowlist)
```

A cotação PTAX (Banco Central — `olinda.bcb.gov.br`) está fora do allowlist de egress, então o
header exibe "Cotação PTAX indisponível" e USD/CNY ficam `R$ —`. Não bloqueia o fluxo (cálculo
usa BRL; USD ficou US$ 0,00 nos itens). Erros pontuais e **esperados** observados: `400` no
`POST /auth/v1/token` (tentativa deliberada de senha errada, validando R1) e `400` no
`POST /rpc/mark_bl_ready_for_billing` com mensagem de negócio *"B/L exige reconciliacao manual
antes do faturamento."* (regra correta — B/L ainda não reconciliado).

#### Observações menores adicionais

- **Importação de veículos**: ao confirmar, o modal fecha sem exibir um relatório de resultado
  ("2 success") — o preview mostra "2 válidas / 0 erros", mas não há toast/resumo pós-persistência.
- **`/containers?search=...`**: o parâmetro de busca via URL não é aplicado (lista retornou os
  2.286 registros; o filtro tem de ser acionado na própria tela).

#### Screenshots

- `docs/assets/qa-step1-painel.png` — painel pós-login.
- `docs/assets/qa-invoice-print.png` — modal "Imprimir INV-2026-0041" (FATURA DE TAXAS LOCAIS).

### 5.7 Correções aplicadas (pós-auditoria)

| Achado | Correção | Arquivo(s) |
|---|---|---|
| **R1** | `handleSubmit` agora distingue erro de transporte (`TypeError`/`AuthRetryableFetchError`/`status 0`) de credencial inválida, exibindo "Serviço de autenticação indisponível…" em vez de mascarar como senha errada. | `src/pages/Login.tsx` |
| **R2** | `queryKeys.charges.operations()` (sem filtros) passa a retornar o **prefixo base** `['local-charge-operations']`; antes retornava `[..., undefined]`, que o React Query v5 não casa por prefixo — por isso a grade de Validação não atualizava após `Recalcular selecionados`. | `src/services/queryKeys.ts` |
| **R3** | `calculate_bl_local_charges` (RPC) agora atualiza `bls.billing_hold_reason` conforme o desfecho do cálculo (limpa quando calcula OK / isento; mantém mensagem coerente em review/USD/sem-tabela). Antes o recálculo deixava o bloqueio obsoleto. | migration `20260612144751_fix_recalc_clears_billing_hold_reason.sql` (aplicada) |
| **R4** | O fuzzy match de cliente por nome (Levenshtein ≥ 0,90) agora exige que o **token distintivo** (primeira palavra) coincida — evita falso-positivo entre nomes que só compartilham termos genéricos (ex.: "QA COMERCIO EXTERIOR" × "AJ COMERCIO EXTERIOR"). +2 testes. | `src/services/customerReconciliation.ts`, `src/services/__tests__/customerReconciliation.test.ts` |
| **U1** | A aba Cobranças do B/L ganhou ação **"Calcular taxas"** (calcula o próprio B/L via `useCalculateBlLocalCharges`), substituindo o link sem saída "Calcular em lote em Taxas Locais →". | `src/components/bl/BlCobrancasTab.tsx`, `src/hooks/useLocalCharges.ts` |

Validação: `tsc -b`, `eslint .` e `vitest run` (**383 passed / 9 skipped**) + `vite build` verdes.

## 6. Saúde geral

- `npm test`: **381 passed, 9 skipped** — suíte verde.
- Migrations aplicadas no projeto remoto até `20260610163251`.
- Tabelas de tarifas ativas: Vitória CNTR/BB e Salvador CNTR 2026.
