# Correções pós-merge da PR #386 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corrigir os bugs e inconsistências encontrados na revisão da PR #386 (gate UX de provisionamento pré-piloto), já mergeada em `main`.

**Architecture:** Correções pontuais em `PortalReviewPanel`, `ClienteFicha`, hooks de provisionamento e uma migration corretiva (`197`) sobre o read model `portal_list_provisioning_console`. Nenhuma feature nova.

**Tech Stack:** React + TypeScript, TanStack React Query, Supabase (plpgsql), Vitest.

---

## Achados da revisão (resumo)

| # | Severidade | Achado | Local |
|---|---|---|---|
| 1 | **Alta** | `al.entity_id::bigint` na checagem de alertas de invoice pode abortar a RPC inteira com erro de cast (`22P02`): a migration `168` grava `invoice_number` (texto não numérico) em `alerts.entity_id`. Qualquer alerta de invoice não numérico derruba a fila para todos. | `supabase/migrations/196_portal_provisioning_console_read_model.sql:26` |
| 2 | **Alta** | `invalidatePortalQueries` invalida `['customer-detail', customerId]` (id numérico), mas a chave real de `useCustomerDetail` é `['customer-detail', cnpj]` (string). A invalidação nunca acerta — a ficha do Cliente fica stale após ações do Portal. | `src/hooks/usePortalProvisioning.ts:43-46` |
| 3 | **Alta** | `const [busy] = useState(false)` — `busy` é sempre `false`; nenhum botão desabilita durante mutações. Duplo clique dispara convite/suspensão duas vezes. | `src/components/portal/PortalReviewPanel.tsx` |
| 4 | **Média** | `changeDecision` chama `mutateAsync` sem `try/catch`: erro vira unhandled rejection, o usuário não vê mensagem (todos os outros handlers setam `error`). | `src/components/portal/PortalReviewPanel.tsx` |
| 5 | **Média** | Bloco de resumo duplicado e morto em `ClienteFicha` com classes conflitantes `hidden mt-4 grid` (sobra de refactor) e usando `recovery_email_source` cru. | `src/pages/ClienteFicha.tsx:285-289` |
| 6 | **Média** | Reativação de conta exige email no formulário, mas o email nunca é enviado ao edge function `portal-account-suspend` (payload só tem `customer_id`, `action`, `reason`). Ou a validação é morta, ou falta o campo no payload — verificar contrato do edge function. | `src/components/portal/PortalReviewPanel.tsx` (`changeAccount`) |
| 7 | **Média** | Gating do perfil Operações feito por classe CSS `hidden`: as seções (input de email, candidatos, ações administrativas, histórico) continuam montadas no DOM. O servidor anula dados sensíveis e bloqueia RPCs, então não há vazamento, mas é frágil — trocar por render condicional. | `src/components/portal/PortalReviewPanel.tsx` |
| 8 | **Baixa** | Código morto: `listPortalProvisioning` não tem mais chamadores; `QueueRow.sharedEmailCnpjs` é sempre `[]` e não é mais lido em produção. | `src/services/portalProvisioning.ts` |
| 9 | **Baixa** | `Clientes.tsx` busca a fila inteira do Portal só para o badge de contagem; para perfis fora dos quatro autorizados a RPC lança `permission denied` e o React Query fica re-tentando. | `src/pages/Clientes.tsx` |
| 10 | **Baixa** | Export XLSX usa nome fixo `portal-clientes.xlsx`, sem `makeTimestamp()` como os demais exports. | `src/services/exports.ts` |
| 11 | **Baixa** | Card "Convites pendentes" seleciona o preset `convite_pendente`, que não existe na barra de abas — filtro ativo sem aba destacada. | `src/pages/ClientesPortal.tsx` |
| 12 | **Baixa** | `rowRefs` (Map) nunca remove entradas de linhas desmontadas — refs stale acumulam. | `src/pages/ClientesPortal.tsx` |
| 13 | **Nota** | `has_active_process` usa `b.financial_status <> 'cancelled'`, que exclui BLs com `financial_status IS NULL`. Confirmar se é intencional; se não, usar `IS DISTINCT FROM`. Tratado na migration 197. | migration `196` |
| 14 | **Nota** | Docs (`docs/modules/portal-cliente.md`, `seguranca.md`) dizem que Operações vê "apenas situação resumida", mas a RPC entrega `has_open_invoice`/`has_active_process` a Operações. Alinhar doc ou projeção. | docs + migration |

---

### Task 1: Migration 197 — corrigir cast de `entity_id` e `has_active_process`

**Files:**
- Create: `supabase/migrations/197_portal_provisioning_console_fixes.sql`
- Test: `src/services/__tests__/portalProvisioningConsoleReadModelMigration.test.ts`

- [ ] **Step 1: Escrever teste que falha**

Adicionar ao describe existente em `src/services/__tests__/portalProvisioningConsoleReadModelMigration.test.ts`:

```ts
const sql197 = readFileSync(resolve(process.cwd(), 'supabase/migrations/197_portal_provisioning_console_fixes.sql'), 'utf8')

it('197 corrige o cast de entity_id e trata financial_status nulo', () => {
  expect(sql197).toMatch(/invoice_number = al\.entity_id/i)
  expect(sql197).toMatch(/al\.entity_id ~ '\^\[0-9\]\+\$'/)
  expect(sql197).toMatch(/IS DISTINCT FROM 'cancelled'/i)
  expect(sql197).not.toMatch(/al\.entity_id::bigint AND/i)
})
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Run: `npx vitest run src/services/__tests__/portalProvisioningConsoleReadModelMigration.test.ts`
Expected: FAIL (arquivo 197 não existe).

- [ ] **Step 3: Criar a migration 197**

`supabase/migrations/197_portal_provisioning_console_fixes.sql` — recria a função com o corpo da `196`, mudando apenas as duas expressões:

```sql
-- 197: Corrige cast de alerts.entity_id (invoice_number textual, migration 168)
-- e inclui BLs com financial_status nulo em has_active_process.
CREATE OR REPLACE FUNCTION public.portal_list_provisioning_console(p_customer_id BIGINT DEFAULT NULL)
RETURNS SETOF JSONB LANGUAGE plpgsql STABLE SECURITY DEFINER SET search_path TO 'public', 'pg_temp' AS $$
DECLARE
  v_role TEXT := public._portal_actor_role();
  v_full_access BOOLEAN := v_role IN ('administrativo','documentacao','financeiro');
BEGIN
  IF v_role IS NULL OR v_role NOT IN ('administrativo','documentacao','financeiro','operacoes') THEN
    RAISE EXCEPTION 'permission denied' USING ERRCODE='42501';
  END IF;

  RETURN QUERY
  SELECT jsonb_build_object(
    'account_id', a.id,
    'customer_id', c.id,
    'customer_name', c.name,
    'cnpj_cpf', c.cnpj_cpf,
    'provisioning_decision', a.provisioning_decision,
    'account_situation', CASE WHEN a.account_situation = 'convite_pendente' AND pi.expires_at < now() THEN 'convite_expirado' ELSE a.account_situation END,
    'recovery_email', CASE WHEN v_full_access THEN a.recovery_email ELSE NULL END,
    'recovery_email_source', CASE WHEN v_full_access THEN a.recovery_email_source ELSE NULL END,
    'pending_invite_expires_at', pi.expires_at,
    'latest_delivery_status', CASE WHEN v_full_access THEN ea.status ELSE NULL END,
    'exception_reason', CASE WHEN v_full_access THEN ex.reason ELSE NULL END,
    'last_event_at', ev.created_at,
    'has_critical_alert', EXISTS (
      SELECT 1 FROM public.alerts al
      WHERE al.status <> 'closed'
        AND al.type IN ('portal_excecao_critica_fatura','portal_convite_expirado','portal_falha_envio','portal_abuso_login')
        AND (
          (al.entity_type = 'customer' AND al.entity_id = c.id::text)
          OR (al.entity_type = 'invoice' AND EXISTS (
            SELECT 1 FROM public.invoices ai
            WHERE ai.customer_id = c.id
              AND (ai.invoice_number = al.entity_id
                   OR (al.entity_id ~ '^[0-9]+$' AND ai.id = al.entity_id::bigint))
          ))
        )
    ),
    'has_open_invoice', EXISTS (SELECT 1 FROM public.invoices i WHERE i.customer_id = c.id AND i.status IN ('issued','overdue')),
    'has_active_process', EXISTS (SELECT 1 FROM public.bls b WHERE b.customer_id = c.id AND b.financial_status IS DISTINCT FROM 'cancelled'),
    'candidates', CASE WHEN v_full_access THEN COALESCE((SELECT jsonb_agg(jsonb_build_object('email', cc.email, 'purpose', cc.purpose, 'origin', 'Contato do Cliente') ORDER BY cc.id) FROM public.customer_contacts cc WHERE cc.customer_id = c.id AND cc.email IS NOT NULL AND cc.purpose IN ('geral','financeiro','operacional','faturamento')), '[]'::jsonb) ELSE '[]'::jsonb END,
    'shared_email_count', CASE WHEN v_full_access AND a.recovery_email IS NOT NULL THEN (SELECT count(*) FROM public.customer_portal_accounts other WHERE lower(other.recovery_email) = lower(a.recovery_email) AND other.id <> a.id) ELSE 0 END
  )
  FROM public.customer_portal_accounts a
  JOIN public.customers c ON c.id = a.customer_id
  LEFT JOIN LATERAL (SELECT expires_at FROM public.portal_invites WHERE account_id = a.id AND purpose = 'convite' AND status = 'pendente' ORDER BY created_at DESC LIMIT 1) pi ON true
  LEFT JOIN LATERAL (SELECT status FROM public.portal_email_attempts WHERE account_id = a.id ORDER BY created_at DESC LIMIT 1) ea ON true
  LEFT JOIN LATERAL (SELECT reason, created_at FROM public.portal_provisioning_events WHERE customer_id = c.id ORDER BY created_at DESC LIMIT 1) ev ON true
  LEFT JOIN LATERAL (SELECT reason FROM public.portal_provisioning_events WHERE customer_id = c.id AND new_decision = 'provisionamento_nao_necessario' ORDER BY created_at DESC LIMIT 1) ex ON true
  WHERE p_customer_id IS NULL OR c.id = p_customer_id
  ORDER BY c.name;
END; $$;
GRANT EXECUTE ON FUNCTION public.portal_list_provisioning_console(BIGINT) TO authenticated;
REVOKE EXECUTE ON FUNCTION public.portal_list_provisioning_console(BIGINT) FROM PUBLIC, anon;
```

- [ ] **Step 4: Rodar o teste e ver passar**

Run: `npx vitest run src/services/__tests__/portalProvisioningConsoleReadModelMigration.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/197_portal_provisioning_console_fixes.sql src/services/__tests__/portalProvisioningConsoleReadModelMigration.test.ts
git commit -m "fix(portal): corrige cast de entity_id e financial_status nulo no read model"
```

---

### Task 2: Corrigir invalidação da ficha do Cliente (chave errada)

**Files:**
- Modify: `src/hooks/usePortalProvisioning.ts` (função `invalidatePortalQueries`)

- [ ] **Step 1: Trocar a invalidação por chave-prefixo correta**

A chave real de `useCustomerDetail` é `['customer-detail', cnpj]` (string). O hook de mutação só conhece o `customerId` numérico, então invalide o prefixo inteiro:

```ts
function invalidatePortalQueries(queryClient: ReturnType<typeof useQueryClient>) {
  void queryClient.invalidateQueries({ queryKey: PORTAL_PROVISIONING_QUERY_KEY })
  void queryClient.invalidateQueries({ queryKey: ['customer-detail'] })
}
```

Atualizar os quatro `onSuccess` para `onSuccess: () => invalidatePortalQueries(queryClient)` (o parâmetro `variables` deixa de ser usado).

- [ ] **Step 2: Verificar**

Run: `npx vitest run src/services/__tests__/portalProvisioning.test.ts src/components/portal/__tests__/PortalReviewPanel.test.tsx && npx tsc --noEmit`
Expected: PASS, sem erros de tipo.

- [ ] **Step 3: Commit**

```bash
git add src/hooks/usePortalProvisioning.ts
git commit -m "fix(portal): invalida customer-detail com prefixo correto após ações"
```

---

### Task 3: `busy` real e tratamento de erro em `changeDecision`

**Files:**
- Modify: `src/components/portal/PortalReviewPanel.tsx`

- [ ] **Step 1: Derivar `busy` das mutações**

Remover `const [busy] = useState(false)` e substituir por:

```ts
const busy = sendInviteMutation.isPending || cancelInviteMutation.isPending || suspendMutation.isPending
  || assistedEmailMutation.isPending || exceptionMutation.isPending || returnToAnalysisMutation.isPending
```

- [ ] **Step 2: Envolver `changeDecision` em try/catch como os demais handlers**

```ts
async function changeDecision(action: 'exception' | 'analysis') {
  if (!reason.trim()) { setError('Informe a justificativa.'); return }
  if (action === 'exception') {
    const authorized = await confirm({ title: 'Confirmar exceção de provisionamento', message: 'Esta ação registra que o provisionamento não é necessário no momento. Confirme a justificativa para continuar.', confirmLabel: 'Registrar exceção' })
    if (!authorized) return
  }
  try {
    if (action === 'exception') await exceptionMutation.mutateAsync({ customerId: row.customer_id, reason: reason.trim() })
    else await returnToAnalysisMutation.mutateAsync({ customerId: row.customer_id, reason: reason.trim() })
    showToast('Decisão do Portal atualizada.', 'success'); onSaved?.()
  } catch (err) { setError(err instanceof Error ? err.message : 'Não foi possível atualizar a decisão.') }
}
```

- [ ] **Step 3: Verificar**

Run: `npx vitest run src/components/portal/__tests__/PortalReviewPanel.test.tsx && npm run lint`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add src/components/portal/PortalReviewPanel.tsx
git commit -m "fix(portal): desabilita botões durante mutações e trata erro de decisão"
```

---

### Task 4: Remover bloco morto duplicado em `ClienteFicha`

**Files:**
- Modify: `src/pages/ClienteFicha.tsx:285-289`

- [ ] **Step 1: Deletar o primeiro bloco `{portalRow ? <div className="hidden mt-4 grid ...` (linhas 285-289)** — é duplicata morta do bloco seguinte, com classes conflitantes (`hidden` + `grid`) e `recovery_email_source` sem label.

- [ ] **Step 2: Verificar**

Run: `npx vitest run src/pages/__tests__/ClienteFicha.behavior.test.tsx`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add src/pages/ClienteFicha.tsx
git commit -m "fix(clientes): remove bloco duplicado morto do resumo do Portal"
```

---

### Task 5: Resolver validação de email na reativação

**Files:**
- Modify: `src/components/portal/PortalReviewPanel.tsx` (`changeAccount`)
- Ler antes: `supabase/functions/portal-account-suspend/index.ts` (contrato real do edge function)

- [ ] **Step 1: Ler o edge function e decidir**

Se `portal-account-suspend` aceita/usa um email na reativação, incluí-lo no payload da mutação (`useSuspendPortalAccount`) e no corpo enviado. Se o edge function deriva o email do `recovery_email` já registrado, **remover** a validação `if (action === 'reactivate' && !email.trim())` e o texto de ajuda "Reativar exige email" — hoje ela bloqueia a reativação sem efeito algum no backend.

- [ ] **Step 2: Verificar**

Run: `npx vitest run src/components/portal/__tests__/PortalReviewPanel.test.tsx`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add -A src/components/portal src/hooks/usePortalProvisioning.ts
git commit -m "fix(portal): alinha validação de email da reativação ao contrato do edge function"
```

---

### Task 6: Render condicional para Operações (em vez de `hidden`)

**Files:**
- Modify: `src/components/portal/PortalReviewPanel.tsx`

- [ ] **Step 1: Trocar cada `className={\`... ${isOperations ? 'hidden' : ''}\`}` por render condicional**

Padrão: envolver a seção em `{!isOperations ? ( ... ) : null}` mantendo o JSX interno idêntico. Aplica-se a: seção "Candidatos de email", grid do formulário de email, `<details>` "Ações administrativas" e seção "Histórico".

- [ ] **Step 2: Verificar**

Run: `npx vitest run src/components/portal/__tests__/PortalReviewPanel.test.tsx && npm run lint`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add src/components/portal/PortalReviewPanel.tsx
git commit -m "refactor(portal): não monta seções restritas para o perfil operacoes"
```

---

### Task 7: Limpezas — código morto, badge com role-guard, export com timestamp, aba `convite_pendente`, refs stale

**Files:**
- Modify: `src/services/portalProvisioning.ts` — remover `listPortalProvisioning`, `toBaseRow` pode ser absorvido; remover `sharedEmailCnpjs` de `QueueRow` e dos fixtures de teste (`src/services/__tests__/portalProvisioning.test.ts`, `src/components/portal/__tests__/PortalReviewPanel.test.tsx`, `src/pages/__tests__/ClientesPortal.behavior.test.tsx`, `src/lib/__tests__/portalProvisioningViewModel.test.ts`).
- Modify: `src/pages/Clientes.tsx` — só buscar a fila quando o perfil pode ler:

```ts
const { effectiveRole } = useAuth()
const canSeePortalQueue = ['administrativo', 'documentacao', 'financeiro', 'operacoes'].includes(effectiveRole ?? '')
const { data: portalRows } = usePortalProvisioning(canSeePortalQueue)
```

  e em `usePortalProvisioning` aceitar `enabled = true` como parâmetro: `useQuery({ queryKey: PORTAL_PROVISIONING_QUERY_KEY, queryFn: () => listPortalProvisioningQueue(), enabled })`.
- Modify: `src/services/exports.ts` — `XLSX.writeFile(workbook, \`portal-clientes-${makeTimestamp()}.xlsx\`)`.
- Modify: `src/pages/ClientesPortal.tsx` — adicionar `{ value: 'convite_pendente', label: 'Convites pendentes' }` à lista `presets` (depois de "Aguardando análise"); trocar o ref callback por versão com cleanup:

```tsx
ref={(element) => { if (element) rowRefs.current.set(row.customer_id, element); else rowRefs.current.delete(row.customer_id) }}
```

- [ ] **Step 1: Aplicar as modificações acima**

- [ ] **Step 2: Verificar**

Run: `npx vitest run src/services/__tests__ src/pages/__tests__ src/components/portal/__tests__ src/lib/__tests__ && npm run lint && npx tsc --noEmit`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add -A src
git commit -m "chore(portal): remove código morto e ajusta badge, export, presets e refs"
```

---

### Task 8: Alinhar documentação (item 14) e verificação final

**Files:**
- Modify: `docs/modules/portal-cliente.md`, `docs/operations/seguranca.md` — registrar que Operações também recebe os booleanos `has_open_invoice`/`has_active_process` (ou, se a decisão for restringir, incluir a mudança na migration 197 zerando-os para `operacoes`). Registrar a migration `197` onde a `196` é citada (`docs/ARCHITECTURE.md`, `docs/RASTREABILIDADE.md`, `docs/operations/seguranca.md`).

- [ ] **Step 1: Atualizar os documentos**

- [ ] **Step 2: Verificação completa**

Run: `npm run docs:check && npm run lint && npm test && npm run build`
Expected: tudo PASS

- [ ] **Step 3: Aplicar a migration 197 no ambiente alvo** (`supabase db push` conforme `WORKFLOW.md`) e registrar em `docs/operations/validacao.md`.

- [ ] **Step 4: Commit**

```bash
git add docs
git commit -m "docs(portal): registra migration 197 e projeção real do perfil operacoes"
```
