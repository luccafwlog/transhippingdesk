# Correções de follow-up da PR #344 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tornar visível a falha inesperada do faturamento automático disparado pelo CE Mercante e corrigir a robustez do parser de telefone do B/L, sem alterar o comportamento de negócio validado no grilling.

**Architecture:** A automação pós-CE (`maybeAutoBillAfterCeMercante`) hoje é *fire-and-forget* com o erro engolido por `.catch(() => {})`. Passamos a: (a) distinguir uma falha inesperada (exceção capturada) de um bloqueio de negócio esperado, via um flag `unexpected` no resultado; (b) registrar a falha inesperada no Histórico do B/L (`logOperationalEvent`) e, no caminho de edição da ficha, também num toast; (c) fazer um short-circuit por `financial_status` que trata o reimport de CE de um B/L já faturado como no-op benigno, registrado como info. O parser de telefone deixa de capturar através de quebras de linha. Um comentário `ponytail:` documenta o teto conhecido de `parseTotalPackages`.

**Tech Stack:** TypeScript, React, TanStack Query, Supabase (Postgres RPC), Vitest, ESLint.

**Contexto do grilling (fundamentos, não reabrir):**
- `is_imo` aplicado a todos os containers do B/L é **comportamento documentado** (`CONTEXT.md`, verbete "Flags Operacionais", linhas 94–99). O Baplie **sempre** precede o cadastro do CE Mercante (confirmado pela operação), então o `is_imo` "cru" do conhecimento nunca chega a um cálculo de fatura — o Baplie já refinou os flags antes do gatilho. **Nenhuma mudança de código.**
- O duplo faturamento é **impossível**: `create_invoice_from_bls_core` (`supabase/migrations/025_billing_orchestration_portal.sql`) levanta exceção quando `financial_status <> 'pending'` ou quando o B/L já está em `invoice_bls`. A idempotência é garantida no banco. **Nenhuma mudança de guard.** Este plano só trata a *observabilidade* dessa falha.
- A migration `171_bl_import_edi_fields.sql` foi verificada com `diff` contra a `166`: é puramente aditiva (colunas `total_packages`, `packages_unit`, `consignee_phone` conectadas de forma idêntica em 7 pontos), sem branch removido. **Nenhuma mudança.**

---

## File Structure

- `src/services/operationalEvents.ts` — **Modify.** Adiciona dois códigos ao union `OperationalEventCode`. Responsabilidade inalterada: catálogo tipado de eventos operacionais gravados em `audit_logs`.
- `src/services/reviewBillingAutomation.ts` — **Modify.** Núcleo da mudança: flag `unexpected` no resultado, short-circuit por `financial_status` e logging dentro de `maybeAutoBillAfterCeMercante`.
- `src/services/__tests__/reviewBillingAutomation.test.ts` — **Modify.** Um assert existente muda (catch agora carrega `unexpected: true`); novos testes cobrem `maybeAutoBillAfterCeMercante`.
- `src/hooks/useBlEditForm.ts` — **Modify.** Consome o resultado da automação para exibir toast na falha inesperada (caminho de edição da ficha).
- `src/services/blFreightImport.ts` — **Modify.** `extractPhone` deixa de capturar através de `\n`.
- `src/services/__tests__/blFreightImport.test.ts` — **Modify.** Teste do telefone multi-linha.
- `src/services/blParser.ts` — **Modify.** Comentário `ponytail:` em `parseTotalPackages` (sem mudança de lógica).
- `docs/modules/taxas-locais.md` — **Modify.** Atualiza a linha da automação com o novo comportamento de observabilidade.
- `docs/RASTREABILIDADE.md` — **Modify.** Reflete os arquivos/serviços tocados no traço da automação.

---

### Task 1: Novos códigos de evento operacional

**Files:**
- Modify: `src/services/operationalEvents.ts:4-11`

- [ ] **Step 1: Adicionar os dois códigos ao union**

Em `src/services/operationalEvents.ts`, o union `OperationalEventCode` atualmente termina em `'invoice_cancel_blocked'`. Adicione duas linhas:

```ts
type OperationalEventCode =
  | 'manifest_import_rate_limited'
  | 'manifest_import_duplicate_hash'
  | 'bl_review_concurrent_conflict'
  | 'invoice_create_conflict'
  | 'invoice_payment_invalid'
  | 'invoice_cancel_blocked'
  | 'bl_auto_billing_failed'
  | 'ce_reimport_already_invoiced'
```

- [ ] **Step 2: Verificar compilação**

Run: `npx tsc -b`
Expected: sem erros novos (o arquivo apenas amplia um union de string literais).

- [ ] **Step 3: Commit**

```bash
git add src/services/operationalEvents.ts
git commit -m "feat: adiciona codigos de evento para observabilidade do auto-billing pos-CE"
```

---

### Task 2: Observabilidade da automação de faturamento pós-CE

**Files:**
- Modify: `src/services/reviewBillingAutomation.ts:8-10` (tipo), `:56-58` (catch), `:61-81` (`maybeAutoBillAfterCeMercante`)
- Test: `src/services/__tests__/reviewBillingAutomation.test.ts`

Regras que os testes fixam:
- Uma **falha inesperada** (exceção capturada por `tryAutoIssueInvoice`) devolve `{ status: 'blocked', message, unexpected: true }`.
- Um **bloqueio de negócio** (CE ausente, revisão pendente, isento, sem valor) continua `{ status: 'blocked', message }` — **sem** `unexpected`.
- `maybeAutoBillAfterCeMercante` de um B/L **já faturado** (`financial_status <> 'pending'`) **não** chama cálculo/emissão, registra `ce_reimport_already_invoiced` e devolve `null`.
- `maybeAutoBillAfterCeMercante` cujo `tryAutoIssueInvoice` volta com `unexpected` registra `bl_auto_billing_failed`.

- [ ] **Step 1: Atualizar o teste de exceção existente e adicionar o mock de eventos**

Em `src/services/__tests__/reviewBillingAutomation.test.ts`:

(a) Adicione o mock de `operationalEvents` junto aos outros `vi.mock` no topo (após o mock de `../billing`, antes do `vi.hoisted`):

```ts
const { mockLogOperationalEvent } = vi.hoisted(() => ({
  mockLogOperationalEvent: vi.fn(),
}))

vi.mock('../operationalEvents', () => ({
  logOperationalEvent: mockLogOperationalEvent,
}))
```

(b) Importe também `maybeAutoBillAfterCeMercante`:

```ts
import { maybeAutoBillAfterCeMercante, tryAutoIssueInvoice } from '../reviewBillingAutomation'
```

(c) No teste existente `'retorna bloqueio com a mensagem da falha da invoice sem propagar excecao'` (hoje em `:155-161`), o `catch` passa a marcar `unexpected: true`. Atualize o assert:

```ts
  it('retorna bloqueio com a mensagem da falha da invoice sem propagar excecao', async () => {
    mockedCreateInvoice.mockRejectedValueOnce(new Error('ledger failed'))

    const result = await tryAutoIssueInvoice({ blId: 'BL1', customerId: 99, actorId: 'user-1' })

    expect(result).toEqual({ status: 'blocked', message: 'ledger failed', unexpected: true })
  })
```

- [ ] **Step 2: Escrever os testes de `maybeAutoBillAfterCeMercante`**

Adicione um novo bloco `describe` ao final do arquivo (após o `describe('tryAutoIssueInvoice', ...)`). O `mockFrom` devolve um superset com os campos lidos pelos dois `select` (o de `maybeAutoBillAfterCeMercante` e o de `tryAutoIssueInvoice`):

```ts
describe('maybeAutoBillAfterCeMercante', () => {
  function mockBl(overrides: Record<string, unknown>) {
    mockFrom.mockImplementation(() => ({
      select: () => ({
        eq: () => ({
          single: async () => ({
            data: {
              id: 'BL1',
              customer_id: 99,
              customer_reconciliation_status: 'matched_document',
              cargo_mode: 'container',
              financial_status: 'pending',
              ce_mercante: '122605051526081',
              ...overrides,
            },
            error: null,
          }),
        }),
      }),
    }))
  }

  it('registra info e nao refatura quando o B/L ja esta faturado', async () => {
    mockBl({ financial_status: 'invoiced' })

    const result = await maybeAutoBillAfterCeMercante('BL1', 'user-1')

    expect(result).toBeNull()
    expect(mockedCalculate).not.toHaveBeenCalled()
    expect(mockedCreateInvoice).not.toHaveBeenCalled()
    expect(mockLogOperationalEvent).toHaveBeenCalledWith(
      expect.objectContaining({ code: 'ce_reimport_already_invoiced', entityId: 'BL1', changedBy: 'user-1' }),
    )
  })

  it('registra falha inesperada quando a emissao lanca erro', async () => {
    mockBl({})
    mockedCreateInvoice.mockRejectedValueOnce(new Error('ledger failed'))

    const result = await maybeAutoBillAfterCeMercante('BL1', 'user-1')

    expect(result).toEqual({ status: 'blocked', message: 'ledger failed', unexpected: true })
    expect(mockLogOperationalEvent).toHaveBeenCalledWith(
      expect.objectContaining({ code: 'bl_auto_billing_failed', entityId: 'BL1', changedBy: 'user-1' }),
    )
  })

  it('nao registra evento quando o bloqueio e de negocio (sem valor faturavel)', async () => {
    mockBl({})
    mockedCalculate.mockResolvedValueOnce({
      bl_id: 'BL1',
      status: 'calculated',
      table_id: 1,
      line_count: 1,
      total_brl: 0,
      total_usd: 0,
      review_required: false,
      exempt: false,
      reason: '',
    })

    const result = await maybeAutoBillAfterCeMercante('BL1', 'user-1')

    expect(result).toMatchObject({ status: 'blocked', message: 'B/L sem valor faturavel apos recalculo.' })
    expect(result).not.toHaveProperty('unexpected')
    expect(mockLogOperationalEvent).not.toHaveBeenCalled()
  })
})
```

- [ ] **Step 3: Rodar os testes e ver falhar**

Run: `npx vitest run src/services/__tests__/reviewBillingAutomation.test.ts`
Expected: FAIL — `maybeAutoBillAfterCeMercante` ainda não seleciona `financial_status` nem loga; o catch ainda não marca `unexpected`.

- [ ] **Step 4: Implementar o tipo e o catch em `tryAutoIssueInvoice`**

Em `src/services/reviewBillingAutomation.ts`, adicione o import de `logOperationalEvent` (junto aos outros imports do topo):

```ts
import { logOperationalEvent } from './operationalEvents'
```

Amplie o tipo do resultado (`:8-10`) com o flag opcional:

```ts
export type ReviewBillingAutomationResult =
  | { status: 'invoiced'; invoiceResult: unknown }
  | { status: 'blocked'; message: string; calculation?: LocalChargeCalculationResult; unexpected?: boolean }
```

No bloco `catch` de `tryAutoIssueInvoice` (`:56-58`), marque a exceção como inesperada:

```ts
  } catch (error) {
    return {
      status: 'blocked',
      message: error instanceof Error ? error.message : 'Falha ao gerar invoice automatica.',
      unexpected: true,
    }
  }
```

- [ ] **Step 5: Implementar short-circuit e logging em `maybeAutoBillAfterCeMercante`**

Substitua toda a função `maybeAutoBillAfterCeMercante` (`:61-81`) por:

```ts
export async function maybeAutoBillAfterCeMercante(blId: string, actorId: string | null) {
  const { data, error } = await supabase
    .from('bls')
    .select('id, customer_id, customer_reconciliation_status, cargo_mode, financial_status')
    .eq('id', blId)
    .single()
  if (error) throw error

  const bl = data as {
    id: string
    customer_id: number | null
    customer_reconciliation_status: string | null
    cargo_mode: string | null
    financial_status: string | null
  } | null

  if (!bl?.customer_id || bl.customer_reconciliation_status !== 'matched_document') return null
  const cargoMode = bl.cargo_mode ?? 'container'
  if (cargoMode !== 'container' && cargoMode !== '') return null

  // Reimport de CE em B/L ja faturado e no-op benigno: create_invoice_from_bls_core
  // recusaria a segunda fatura. Registramos como info e nao tentamos refaturar.
  if ((bl.financial_status ?? 'pending') !== 'pending') {
    await logOperationalEvent({
      code: 'ce_reimport_already_invoiced',
      message: `Reimport de CE Mercante em B/L ja faturado (${bl.financial_status}); refaturamento ignorado.`,
      changedBy: actorId,
      entityId: bl.id,
      context: { source: 'ce_auto_billing', financial_status: bl.financial_status },
    })
    return null
  }

  const result = await tryAutoIssueInvoice({ blId: bl.id, customerId: bl.customer_id, actorId })
  if (result.status === 'blocked' && result.unexpected) {
    await logOperationalEvent({
      code: 'bl_auto_billing_failed',
      message: result.message,
      changedBy: actorId,
      entityId: bl.id,
      context: { source: 'ce_auto_billing' },
    })
  }
  return result
}
```

- [ ] **Step 6: Rodar os testes e ver passar**

Run: `npx vitest run src/services/__tests__/reviewBillingAutomation.test.ts`
Expected: PASS (todos, incluindo o teste `'ledger failed'` atualizado e os três novos).

- [ ] **Step 7: Commit**

```bash
git add src/services/reviewBillingAutomation.ts src/services/__tests__/reviewBillingAutomation.test.ts
git commit -m "feat: torna visivel a falha do auto-billing pos-CE e loga reimport de B/L ja faturado"
```

---

### Task 3: Toast na ficha do B/L quando a automação falha

**Files:**
- Modify: `src/hooks/useBlEditForm.ts:168-173`

`useBlEditForm` já importa `maybeAutoBillAfterCeMercante`, `useToast` (`showToast`) e `logOperationalEvent`. O logging no Histórico já acontece dentro de `maybeAutoBillAfterCeMercante` (Task 2); aqui só adicionamos o toast quando a falha for inesperada. Este hook não tem harness de teste unitário no repo; a verificação é `tsc` + smoke manual.

- [ ] **Step 1: Consumir o resultado para exibir o toast**

Substitua o bloco atual (`:168-173`):

```ts
      const ceMercanteAdded = changes.includes('ce_mercante')
        && !stringifyValue(baselineForm?.ce_mercante).trim()
        && Boolean(stringifyValue(form.ce_mercante).trim())
      if (ceMercanteAdded) {
        void maybeAutoBillAfterCeMercante(bl.id, user.id).catch(() => {})
      }
```

por:

```ts
      const ceMercanteAdded = changes.includes('ce_mercante')
        && !stringifyValue(baselineForm?.ce_mercante).trim()
        && Boolean(stringifyValue(form.ce_mercante).trim())
      if (ceMercanteAdded) {
        void maybeAutoBillAfterCeMercante(bl.id, user.id)
          .then((result) => {
            if (result?.status === 'blocked' && result.unexpected) {
              showToast(
                'CE Mercante salvo, mas o faturamento automático falhou. Verifique o Histórico do B/L.',
                'error',
              )
            }
          })
          .catch(() => {})
      }
```

- [ ] **Step 2: Verificar compilação e lint**

Run: `npx tsc -b && npx eslint src/hooks/useBlEditForm.ts`
Expected: sem erros. (`showToast` já está em escopo no hook; o `.then` recebe `ReviewBillingAutomationResult | null`.)

- [ ] **Step 3: Commit**

```bash
git add src/hooks/useBlEditForm.ts
git commit -m "feat: avisa por toast quando o auto-billing pos-CE falha na edicao do B/L"
```

---

### Task 4: `extractPhone` não captura através de quebras de linha

**Files:**
- Modify: `src/services/blFreightImport.ts:621-623`
- Test: `src/services/__tests__/blFreightImport.test.ts`

O `consigneeBlock` é multi-linha. Hoje a classe `[+0-9()\s-]` inclui `\s`, então `{8,25}` pode engolir `\n` + dígitos do próximo campo e devolver um telefone com quebra no meio (o `.trim()` só limpa as bordas). Trocamos `\s` por espaço literal.

- [ ] **Step 1: Escrever o teste que falha**

Em `src/services/__tests__/blFreightImport.test.ts`, dentro do `describe('blFreightImport', ...)`, logo após o teste `'extracts consignee phone and leaves non-DG containers as non-IMO'`, adicione:

```ts
  it('nao captura telefone atravessando quebra de linha do bloco consignee', () => {
    const doc = parsedBL()
    doc.parties.consigneeBlock = 'IMPORTADOR LTDA\nTEL:2124-1654\n3221 0000 CEP 29000-000'

    const payload = buildBlFreightPayload(doc, 7)

    expect(payload.consignee_phone).toBe('2124-1654')
  })
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Run: `npx vitest run src/services/__tests__/blFreightImport.test.ts -t "quebra de linha"`
Expected: FAIL — o valor atual vem como `'2124-1654\n3221 0000'` (contém `\n`), diferente de `'2124-1654'`.

- [ ] **Step 3: Corrigir o regex**

Em `src/services/blFreightImport.ts:621-623`, troque `\s` por espaço literal dentro da classe de captura:

```ts
function extractPhone(value: string) {
  return value.match(/TEL[.:]?\s*([+0-9() -]{8,25})/i)?.[1]?.trim() ?? null
}
```

- [ ] **Step 4: Rodar o arquivo de teste inteiro e ver passar**

Run: `npx vitest run src/services/__tests__/blFreightImport.test.ts`
Expected: PASS — o novo teste passa e o teste existente `'extracts consignee phone...'` (que espera `'+55 27 2124-1654'`) continua passando (todos os caracteres seguem na classe; não há `\n` após o número).

- [ ] **Step 5: Commit**

```bash
git add src/services/blFreightImport.ts src/services/__tests__/blFreightImport.test.ts
git commit -m "fix: extractPhone deixa de capturar telefone atraves de quebras de linha"
```

---

### Task 5: Documentar o teto de `parseTotalPackages`

**Files:**
- Modify: `src/services/blParser.ts:135-145`

Mudança **somente de comentário** — sem alteração de lógica, portanto sem teste (trivial conforme CLAUDE.md §1/§2). `total_packages`/`packages_unit` são campos comerciais/de exibição, sem impacto em faturamento.

- [ ] **Step 1: Adicionar o comentário `ponytail:`**

Em `src/services/blParser.ts`, dentro de `parseTotalPackages`, imediatamente antes da linha `const match = cell(rows, rowNumber, 'C').match(...)`:

```ts
function parseTotalPackages(rows: RawSheetRow[]) {
  for (let rowNumber = 44; rowNumber <= 60; rowNumber += 1) {
    if (cell(rows, rowNumber, 'A').trim() !== 'TOTAL:') continue
    // ponytail: reconhece apenas unidade de palavra unica (UNITS, PACKAGES) e
    // milhar no formato que toNumber entende. Unidade composta ("BIG BAGS") cai
    // para null. Campo comercial/exibicao, sem impacto em faturamento; estender
    // a classe para [A-Z ]+ e normalizar milhar se surgir B/L multi-palavra.
    const match = cell(rows, rowNumber, 'C').match(/^\s*(\d[\d.,]*)\s+([A-Z]+)\s*$/i)
    return {
      totalPackages: match ? parseNumber(match[1]) : null,
      packagesUnit: match ? match[2].toUpperCase() : null,
    }
  }
  return { totalPackages: null, packagesUnit: null }
}
```

- [ ] **Step 2: Verificar lint**

Run: `npx eslint src/services/blParser.ts`
Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add src/services/blParser.ts
git commit -m "docs: documenta o teto de parseTotalPackages com comentario ponytail"
```

---

### Task 6: Atualizar documentação viva

**Files:**
- Modify: `docs/modules/taxas-locais.md:92`
- Modify: `docs/RASTREABILIDADE.md` (linha do traço da automação pós-CE)

CLAUDE.md §6 exige atualizar a documentação viva junto da mudança de comportamento (nova observabilidade da automação).

- [ ] **Step 1: Atualizar a coluna de erros/observações da linha da automação em `taxas-locais.md`**

Em `docs/modules/taxas-locais.md`, a linha 92 (a que começa com `| B/L/revisão · calcular ou recalcular um B/L |`). Na célula de **erros/observações** (a penúltima), acrescente ao final a frase:

> Falha **inesperada** da automação pós-CE é registrada no Histórico do B/L (`bl_auto_billing_failed`) e, na edição da ficha, também num toast; reimport de CE de B/L já faturado é no-op benigno registrado como info (`ce_reimport_already_invoiced`).

E na célula de **Código**, acrescente `src/services/operationalEvents.ts` à lista de arquivos.

- [ ] **Step 2: Refletir os arquivos no `RASTREABILIDADE.md`**

Em `docs/RASTREABILIDADE.md`, localize o traço que já menciona `maybeAutoBillAfterCeMercante` / `reviewBillingAutomation` (grep abaixo) e acrescente, na descrição do efeito, que a falha inesperada gera evento operacional (`bl_auto_billing_failed`) e o reimport de B/L já faturado gera `ce_reimport_already_invoiced`, citando `src/services/operationalEvents.ts`.

Run para localizar as âncoras exatas:
`grep -n "maybeAutoBillAfterCeMercante\|reviewBillingAutomation" docs/RASTREABILIDADE.md`

- [ ] **Step 3: Rodar o verificador de docs**

Run: `npm run docs:check`
Expected: PASS (sem links quebrados nem referências inválidas). Se o verificador exigir que todo arquivo citado exista, confirme que `src/services/operationalEvents.ts` está referenciado corretamente.

- [ ] **Step 4: Commit**

```bash
git add docs/modules/taxas-locais.md docs/RASTREABILIDADE.md
git commit -m "docs: registra observabilidade do auto-billing pos-CE na documentacao viva"
```

---

### Task 7: Verificação final (gates do CLAUDE.md §7)

**Files:** nenhum (apenas execução).

- [ ] **Step 1: Suite completa de testes**

Run: `npm test`
Expected: PASS — nenhuma regressão. Confirme especificamente os arquivos tocados:
`npx vitest run src/services/__tests__/reviewBillingAutomation.test.ts src/services/__tests__/blFreightImport.test.ts src/services/__tests__/ceMercanteImport.test.ts`

- [ ] **Step 2: Lint**

Run: `npm run lint`
Expected: sem erros.

- [ ] **Step 3: Build/typecheck**

Run: `npm run build`
Expected: `tsc -b` e `vite build` sem erros. (Cobre o `ReviewBillingAutomationResult` ampliado e o consumo do `.then` no hook.)

- [ ] **Step 4: Verificador de docs**

Run: `npm run docs:check`
Expected: PASS.

- [ ] **Step 5: Smoke manual do toast (opcional, se o app estiver rodável)**

Abrir a ficha de um B/L container reconciliado por documento, adicionar um CE Mercante e salvar. Caminho feliz: sem toast de erro (a fatura sai ou o bloqueio é de negócio). Para exercitar a falha, forçar um erro transitório no cálculo/emissão e confirmar o toast "CE Mercante salvo, mas o faturamento automático falhou…" e o evento no Histórico do B/L.

---

## Self-Review

**Cobertura do escopo (grilling → tarefas):**
- Item 1 (`is_imo`) → sem tarefa, por decisão registrada nos fundamentos. ✔
- Item 2 (visibilidade da falha + info de reimport) → Tasks 1, 2, 3. ✔
- Item 3 (`extractPhone`) → Task 4. ✔
- Item 4 (migration) → sem tarefa, verificada nos fundamentos. ✔
- Item 5 (`parseTotalPackages`) → Task 5. ✔
- Documentação viva (CLAUDE.md §6) → Task 6. ✔
- Gates (CLAUDE.md §7) → Task 7. ✔

**Consistência de tipos/nomes:** `unexpected?: boolean` é definido no tipo (Task 2, Step 4) e consumido em `maybeAutoBillAfterCeMercante` (Step 5), nos testes (Step 1–2) e no hook (Task 3). Os códigos `bl_auto_billing_failed` e `ce_reimport_already_invoiced` são definidos na Task 1 e usados na Task 2. `logOperationalEvent`, `showToast` e `maybeAutoBillAfterCeMercante` já existem/estão importados nos arquivos-alvo.

**Armadilha conhecida:** o teste existente `'retorna bloqueio com a mensagem da falha da invoice'` usa `toEqual` exato e **quebra** ao adicionar `unexpected: true` — por isso a atualização dele é o Step 1 da Task 2, antes de implementar. Não é regressão; é o assert acompanhando a nova forma do resultado.
