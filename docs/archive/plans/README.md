# Implementation Plans

> **Snapshot histórico:** estes cinco planos foram gerados em 2026-06-15 e
> estão concluídos. O status atual do produto vive em
> [`docs/ROADMAP.md`](../docs/ROADMAP.md).

Generated by the improve skill on 2026-06-15 against commit `1224998`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
the Status column when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Upgrade vulnerable Vite toolchain | P1 | S | - | DONE |
| 002 | Harden anonymous portal login resolution | P1 | M | - | DONE |
| 003 | Fix the portal operation returned-container filter | P1 | S | - | DONE |
| 004 | Make portal billing CSV exports respect active filters | P2 | S | - | DONE |
| 005 | Make dashboard due-soon alerts date-based | P2 | S | - | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- The plans are independent and can be implemented in separate branches.
- Run the full verification suite after each plan because the Portal touches shared Supabase clients, routing, and common UI components.

## Baseline verified during recon

- `npm test` passed: 87 files passed, 1 skipped; 422 tests passed, 9 skipped.
- `npm run lint` passed.
- `npm run build` passed.
- `npm audit --json` reported 2 vulnerabilities: `vite` high and `@babel/core` low.
- After plan execution, `npm audit --audit-level=high` reports 0 vulnerabilities.

## Findings considered and rejected

- `portal_list_notifications` originally applied `LIMIT` after `JSONB_AGG`, but `supabase/migrations/20260615145427_portal_fixes_post_pr227.sql` already rewrites it with a subquery that limits before aggregation.
- `portal_obsolete_consolidation` originally used invalid lifecycle/status values, but `supabase/migrations/20260615145427_portal_fixes_post_pr227.sql` already corrects `event_type = 'obsolete'` and link status `obsolete`.
- `PortalProfile` silently ignores `portalGetProfile()` load failures. This is a UX smell, but lower leverage than the selected plans because profile save still surfaces errors and the critical billing/operation paths are more important.
