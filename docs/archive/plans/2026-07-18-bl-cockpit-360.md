# B/L Cockpit 360° — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transformar a ficha `/manifestos/:blId` no cockpit 360° do B/L: nova aba padrão Visão Geral, pipeline de dois trilhos com próxima ação, aba Detalhes como réplica do documento, ação de COD movida da Viagem para o B/L, e blocos de Transbordo, Baplie e Portal.

**Architecture:** 5 PRs incrementais sobre a estrutura existente (`BlDetalhe.tsx` + `src/components/bl/*`). Lógica nova nasce como função pura testável em `src/services/` (trilhos, status de portal, filtro Baplie) consumida por hooks React Query; escrita auditada continua via `save_bl_review` e RPCs de transbordo existentes. Migrações: `205` (campos documentais + RPCs) e `206` (`portal_notifications.bl_id`), ambas forward-only.

**Tech Stack:** React 18 + TypeScript, React Query v5, Supabase (PostgREST + RPCs plpgsql), Vitest.

**Base de decisão:** `docs/archive/specs/2026-07-18-bl-cockpit-design.md` (sessão grill-me-with-docs de 2026-07-18).

**Regras transversais (valem para todo PR):**
- Branch por PR a partir da default; commits pequenos; PR pronto para review.
- Rodar antes de abrir cada PR: `npm run docs:check && npm run lint && npm test && npm run build`.
- Hooks de `.claude/hooks/` lintam TypeScript editado; `src/types/database.ts` e migrações existentes são protegidos — **criar** migração nova é permitido, **editar** migração existente não.
- Docs vivos atualizados no mesmo PR: `docs/RASTREABILIDADE.md`, `docs/modules/manifesto-edi.md` (anatomia de `/manifestos/:blId`), e `CONTEXT.md` quando indicado.
- Textos de UI em pt-BR, sem acento em literais que o codebase já grava sem acento (seguir vizinhança).

---

## PR 1 — Persistência documental (migração 205 + import)

Persiste os campos que o parser já lê mas descarta: `place_of_receipt`,
`movement_from`, `movement_to`, `issue_place`. Forward-only (sem backfill).

### Task 1.1: Migração `205_bl_document_fields.sql`

**Files:**
- Create: `supabase/migrations/205_bl_document_fields.sql`
- Test: `src/services/__tests__/blDocumentFieldsMigration.test.ts`

- [ ] **Step 1: Escrever o teste da migração (padrão da casa: regex sobre o SQL)**

```ts
// src/services/__tests__/blDocumentFieldsMigration.test.ts
import { readFileSync } from 'node:fs'
import { describe, expect, it } from 'vitest'

const sql = readFileSync('supabase/migrations/205_bl_document_fields.sql', 'utf8')

describe('205_bl_document_fields', () => {
  it('adiciona as colunas documentais em bls', () => {
    for (const col of ['place_of_receipt', 'movement_from', 'movement_to', 'issue_place']) {
      expect(sql).toMatch(new RegExp(`ADD COLUMN IF NOT EXISTS ${col} TEXT`))
    }
  })

  it('reaplica import_bl_freight_transactional cobrindo os novos campos', () => {
    const fn = sql.match(/CREATE OR REPLACE FUNCTION public\.import_bl_freight_transactional[\s\S]*?\$function\$;/i)?.[0] ?? ''
    for (const col of ['place_of_receipt', 'movement_from', 'movement_to', 'issue_place']) {
      // presente na lista de chaves permitidas, na temp table, no SELECT bl->> e no UPDATE/INSERT finais
      expect((fn.match(new RegExp(col, 'g')) ?? []).length).toBeGreaterThanOrEqual(5)
    }
  })

  it('reaplica save_bl_review aceitando os novos campos editaveis', () => {
    const fn = sql.match(/CREATE OR REPLACE FUNCTION public\.save_bl_review[\s\S]*?\$function\$;/i)?.[0] ?? ''
    for (const col of ['place_of_receipt', 'movement_from', 'movement_to', 'issue_place', 'place_of_delivery', 'bl_emission_date']) {
      expect(fn).toMatch(new RegExp(`p_update_payload \\? '${col}'`))
    }
  })
})
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Run: `npx vitest run src/services/__tests__/blDocumentFieldsMigration.test.ts`
Expected: FAIL (arquivo de migração inexistente).

- [ ] **Step 3: Escrever a migração**

Estrutura do arquivo (as funções são cópias integrais das versões vigentes com
os acréscimos indicados — nunca editar 129/171, apenas recriar aqui):

```sql
-- 205_bl_document_fields.sql
-- Campos documentais do B/L que o parser lê e o sistema descartava.
-- Forward-only: B/Ls históricos ficam NULL até reimporte.

ALTER TABLE public.bls ADD COLUMN IF NOT EXISTS place_of_receipt TEXT;
ALTER TABLE public.bls ADD COLUMN IF NOT EXISTS movement_from TEXT;
ALTER TABLE public.bls ADD COLUMN IF NOT EXISTS movement_to TEXT;
ALTER TABLE public.bls ADD COLUMN IF NOT EXISTS issue_place TEXT;

-- 1) import_bl_freight_transactional: copiar a versão vigente de
--    supabase/migrations/171_bl_import_edi_fields.sql e acrescentar os 4 campos
--    novos AO LADO de 'place_of_delivery' em TODOS os pontos onde ela aparece:
--      a) array de chaves permitidas do payload;
--      b) DDL da temp table (TEXT);
--      c) lista de colunas do INSERT na temp table;
--      d) SELECT do payload:  NULLIF(bl->>'place_of_receipt', ''), etc.;
--      e) UPDATE final de public.bls (SET place_of_receipt = t.place_of_receipt, ...);
--      f) INSERT final em public.bls (colunas + valores).

-- 2) save_bl_review: copiar a versão vigente de
--    supabase/migrations/129_review_gate_hardening.sql e acrescentar no UPDATE
--    da whitelist (padrão CASE WHEN p_update_payload ? '<col>'), junto de 'pol':
--      place_of_receipt, movement_from, movement_to, issue_place,
--      place_of_delivery (coluna já existia, entra na whitelist),
--      bl_emission_date (cast ::DATE via NULLIF(p_update_payload->>'bl_emission_date','')::DATE).
```

- [ ] **Step 4: Rodar o teste e ver passar**

Run: `npx vitest run src/services/__tests__/blDocumentFieldsMigration.test.ts`
Expected: PASS.

- [ ] **Step 5: Aplicar a migração no ambiente (MCP Supabase `apply_migration`) conforme `WORKFLOW.md`**

- [ ] **Step 6: Commit**

```bash
git add supabase/migrations/205_bl_document_fields.sql src/services/__tests__/blDocumentFieldsMigration.test.ts
git commit -m "feat(bl): migracao 205 persiste campos documentais do B/L"
```

### Task 1.2: Payload do import carrega os novos campos

**Files:**
- Modify: `src/services/blFreightImport.ts` (payload `buildBlPayload`, ~linha 356, e diffs `addDiff`, ~linha 499)
- Test: `src/services/__tests__/blFreightImport.test.ts` (estender)

- [ ] **Step 1: Teste falhando — payload inclui os campos novos**

No describe existente de payload em `blFreightImport.test.ts`, adicionar:

```ts
it('carrega campos documentais novos no payload', async () => {
  const payload = buildPayloadFromDoc(makeParsedDoc({
    route: { ...baseRoute, receipt: 'SHANGHAI', movementFrom: 'CY', movementTo: 'CY' },
    dates: { ...baseDates, issuePlace: 'SHANGHAI' },
  }))
  expect(payload.place_of_receipt).toBe('SHANGHAI')
  expect(payload.movement_from).toBe('CY')
  expect(payload.movement_to).toBe('CY')
  expect(payload.issue_place).toBe('SHANGHAI')
})
```

(Usar os helpers/factories que o arquivo de teste já possui para `ParsedBLDocument`;
se não houver factory, montar o doc mínimo inline como os testes vizinhos fazem.)

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run src/services/__tests__/blFreightImport.test.ts`

- [ ] **Step 3: Implementar no payload**

Em `blFreightImport.ts`, no objeto retornado por `buildBlPayload` (junto de
`place_of_delivery`):

```ts
place_of_receipt: normalizePortCode(doc.route.receipt),
movement_from: doc.route.movementFrom?.trim() || null,
movement_to: doc.route.movementTo?.trim() || null,
issue_place: doc.dates.issuePlace?.trim() || null,
```

E no cálculo de diffs (junto do `addDiff` de `bl_emission_date`):

```ts
addDiff(diffs, 'place_of_receipt', existing.place_of_receipt, payload.place_of_receipt, false)
addDiff(diffs, 'movement_from', existing.movement_from, payload.movement_from, false)
addDiff(diffs, 'movement_to', existing.movement_to, payload.movement_to, false)
addDiff(diffs, 'issue_place', existing.issue_place, payload.issue_place, false)
```

Ajustar o(s) tipo(s) locais do serviço que tipam o payload/`existing` para
incluir os 4 campos (`string | null`). NÃO tocar `src/types/database.ts`
(protegido): tipar via interface local do serviço, como o arquivo já faz para
campos pós-`database.ts`.

- [ ] **Step 4: Rodar testes do serviço e ver passar**

Run: `npx vitest run src/services/__tests__/blFreightImport.test.ts`

- [ ] **Step 5: Docs + verificação final do PR**

- `docs/modules/manifesto-edi.md`: na linha do catálogo "importar B/L",
  acrescentar os novos campos persistidos.
- `docs/RASTREABILIDADE.md`: linha de `/manifestos` — importar B/L, mesma nota.

Run: `npm run docs:check && npm run lint && npm test && npm run build`

- [ ] **Step 6: Commit + PR**

```bash
git add src/services/blFreightImport.ts src/services/__tests__/blFreightImport.test.ts docs/
git commit -m "feat(bl): import persiste place of receipt, movement e issue place"
```

---

## PR 2 — Aba Detalhes como réplica do documento

Reorganiza o formulário no layout do documento e exibe o que já persiste e
estava invisível: Frete & Despesas, emissão, Place of Delivery, Notify 2,
telefone do consignatário, e os campos novos do PR 1.

### Task 2.1: Campos editáveis novos no `useBlEditForm`

**Files:**
- Modify: `src/hooks/useBlEditForm.ts`
- Test: `src/pages/__tests__/blDetalheHelpers.test.ts` (ou novo `src/hooks/__tests__/useBlEditForm.fields.test.ts`)

- [ ] **Step 1: Teste falhando**

```ts
// src/hooks/__tests__/useBlEditForm.fields.test.ts
import { describe, expect, it } from 'vitest'
import { BL_EDITABLE_FIELDS } from '../useBlEditForm'

describe('BL_EDITABLE_FIELDS', () => {
  it('inclui os campos documentais da replica', () => {
    for (const field of ['place_of_receipt', 'movement_from', 'movement_to', 'issue_place', 'place_of_delivery', 'bl_emission_date']) {
      expect(BL_EDITABLE_FIELDS).toContain(field)
    }
  })
})
```

- [ ] **Step 2: Implementar**

Em `useBlEditForm.ts`:
- Exportar o array (hoje privado): `export const BL_EDITABLE_FIELDS = editableFields`.
- Acrescentar `'place_of_receipt' | 'movement_from' | 'movement_to' | 'issue_place' | 'place_of_delivery' | 'bl_emission_date'` ao `Pick<BL, ...>` e ao array
  `editableFields`. (`place_of_delivery`, `bl_emission_date` e `incoterm` já são
  colunas tipadas em `database.ts`; os 4 campos novos ainda não — usar
  `keyof` sobre um tipo local `BlDocumentFields` intersectado com `BL`, mantendo
  o cast localizado com comentário `ponytail:` até regenerar `database.ts` com
  autorização, mesmo padrão de `src/services/transshipments.ts:5`.)
- Acrescentar os 6 campos em `makeForm` (`bl.place_of_receipt ?? null`, etc.).
- Nenhum deles entra no ramo numérico de `normalizeFormValue`; `bl_emission_date`
  trafega como string `YYYY-MM-DD` de `<input type="date">`.

- [ ] **Step 3: Rodar e ver passar**

Run: `npx vitest run src/hooks/__tests__/useBlEditForm.fields.test.ts`

- [ ] **Step 4: Commit**

```bash
git add src/hooks/useBlEditForm.ts src/hooks/__tests__/useBlEditForm.fields.test.ts
git commit -m "feat(bl): campos documentais editaveis no form auditado"
```

### Task 2.2: Seção Frete & Despesas do BL (leitura)

**Files:**
- Create: `src/components/bl/BlFreightSection.tsx`
- Test: `src/components/bl/__tests__/BlFreightSection.test.tsx`

- [ ] **Step 1: Teste falhando**

```tsx
// src/components/bl/__tests__/BlFreightSection.test.tsx
import { render, screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import { BlFreightSection } from '../BlFreightSection'

const lines = [
  { bl_id: 'BL1', seq: 1, description: 'OCEAN FREIGHT', category: 'OCEAN_FREIGHT', mercante_code: null, currency: 'USD', amount: 1500, payment: 'PREPAID' },
  { bl_id: 'BL1', seq: 2, description: 'THD', category: 'OTHER', mercante_code: null, currency: 'BRL', amount: 900.5, payment: 'COLLECT' },
]

describe('BlFreightSection', () => {
  it('lista as linhas importadas com moeda, valor e prepaid/collect', () => {
    render(<BlFreightSection freightLines={lines} />)
    expect(screen.getByText('OCEAN FREIGHT')).toBeInTheDocument()
    expect(screen.getByText('PREPAID')).toBeInTheDocument()
    expect(screen.getByText('THD')).toBeInTheDocument()
    expect(screen.getByText(/900,5/)).toBeInTheDocument()
  })

  it('mostra vazio orientando o import quando nao ha linhas', () => {
    render(<BlFreightSection freightLines={[]} />)
    expect(screen.getByText(/Nenhuma linha de frete importada/)).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Implementar**

```tsx
// src/components/bl/BlFreightSection.tsx
import { Card } from '../ui/Card'

export type BlFreightLine = {
  bl_id: string
  seq: number
  description: string
  category: string | null
  mercante_code: string | null
  currency: string | null
  amount: number | null
  payment: 'PREPAID' | 'COLLECT' | null
}

const money = (currency: string | null, amount: number | null) =>
  amount == null ? '—' : `${currency ?? ''} ${new Intl.NumberFormat('pt-BR', { minimumFractionDigits: 2 }).format(amount)}`.trim()

// Frete & Despesas do BL: dado declarado pelo armador (fonte do C5 do EDI).
// Distinto de Taxas Locais — somente leitura aqui (CONTEXT.md).
export function BlFreightSection({ freightLines }: { freightLines: BlFreightLine[] }) {
  const sorted = [...freightLines].sort((a, b) => a.seq - b.seq)
  return (
    <Card>
      <h3 className="mb-3 text-sm font-semibold text-[var(--app-text-strong)]">Frete &amp; Despesas do BL</h3>
      {sorted.length === 0 ? (
        <p className="text-sm text-[var(--app-muted)]">Nenhuma linha de frete importada. Use "Importar B/L" para carregar o documento.</p>
      ) : (
        <div className="overflow-x-auto">
          <table className="w-full text-sm">
            <thead>
              <tr className="text-left text-xs uppercase text-[var(--app-muted)]">
                <th className="py-1 pr-3">#</th>
                <th className="py-1 pr-3">Descricao</th>
                <th className="py-1 pr-3">Valor</th>
                <th className="py-1 pr-3">Pagamento</th>
              </tr>
            </thead>
            <tbody>
              {sorted.map((line) => (
                <tr key={line.seq} className="border-t border-[var(--app-border)]">
                  <td className="py-1 pr-3 text-[var(--app-muted)]">{line.seq}</td>
                  <td className="py-1 pr-3">{line.description}</td>
                  <td className="py-1 pr-3">{money(line.currency, line.amount)}</td>
                  <td className="py-1 pr-3">{line.payment ?? '—'}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      )}
    </Card>
  )
}
```

- [ ] **Step 3: Rodar e ver passar; commit**

```bash
npx vitest run src/components/bl/__tests__/BlFreightSection.test.tsx
git add src/components/bl/BlFreightSection.tsx src/components/bl/__tests__/BlFreightSection.test.tsx
git commit -m "feat(bl): secao Frete & Despesas do BL na ficha"
```

### Task 2.3: Reorganizar `BlOperacionalTab` no layout do documento

**Files:**
- Modify: `src/components/bl/BlOperacionalTab.tsx`
- Modify: `src/components/bl/BlDetalhesTab.tsx` (renderizar `BlFreightSection` no modo container)
- Test: `src/pages/__tests__/blTabs.test.tsx` (estender smoke render)

- [ ] **Step 1: Reorganizar o formulário em seções nomeadas, sem mudar o contrato de submit**

Ordem das seções (réplica do documento; campos existentes preservados, novos adicionados):

1. **Partes** — Shipper (edit), Consignatário (edit), Notify Party (edit),
   Notify 2 (`bl.notify2_block`, leitura), Telefone do consignatário
   (`bl.consignee_phone`, leitura).
2. **Rota** — Place of Receipt (edit, novo), POL (edit), POD (edit),
   Place of Delivery (edit, volta à UI), Movement From/To (edit, novos),
   Armador/Navio/Viagem (leitura, vira `<Link>` para `/viagens/${bl.voyage_id}`
   em vez de `<Input disabled>`).
3. **Datas** — Data de emissão (`bl_emission_date`, edit `type="date"`),
   Local de emissão (`issue_place`, edit, novo).
4. **Carga** — NCM chips (leitura), Descrição (edit), Peso/CBM ou campos BB
   (como hoje), DG Class/UN (leitura de `bl.cargo_description` derivados,
   exibidos ao lado dos chips NCM — usar os extratores existentes se públicos;
   se privados no parser, exibir apenas NCM como hoje).
5. **Comercial** — Pagamento PREPAID/COLLECT (edit), CE Mercante (edit).
6. **Revisão & Auditoria** — status, notas, justificativa, salvar (inalterado).

Cada seção é um `<Card>` com `<h3>` do nome. Os novos `<Field>` seguem o padrão
dos existentes:

```tsx
<Field label="Place of Receipt">
  <Input value={form.place_of_receipt ?? ''} onChange={(e) => onFieldChange('place_of_receipt', e.target.value)} />
</Field>
<Field label="Data de emissao">
  <Input type="date" value={(form.bl_emission_date ?? '').slice(0, 10)} onChange={(e) => onFieldChange('bl_emission_date', e.target.value)} />
</Field>
```

Em `BlDetalhesTab.tsx`, após `<BlCargaTab …/>`:

```tsx
{props.isContainerMode ? <BlFreightSection freightLines={props.bl.bl_freight_lines ?? []} /> : null}
```

(`bl_freight_lines` já vem no select de `useBlDetail`; tipar via cast local se
`BLDetail` não expõe — comentário `ponytail:` como em transshipments.)

- [ ] **Step 2: Estender o teste de abas**

Em `blTabs.test.tsx`, garantir que a aba Detalhes renderiza os novos campos:

```tsx
expect(screen.getByLabelText(/Place of Receipt/i)).toBeInTheDocument()
expect(screen.getByLabelText(/Data de emissao/i)).toBeInTheDocument()
expect(screen.getByText(/Frete & Despesas do BL/)).toBeInTheDocument()
```

(Se `Field` não vincula label→input, usar `getByText` no label.)

- [ ] **Step 3: Verificação e docs**

- `docs/modules/manifesto-edi.md` §`/manifestos/:blId`: descrever a réplica
  (seções e campos novos na UI).
- `docs/RASTREABILIDADE.md`: linha "B/L — editar revisão operacional e carga"
  ganha os campos novos.

Run: `npm run docs:check && npm run lint && npm test && npm run build`

- [ ] **Step 4: Commit + PR**

```bash
git add src/components/bl/ src/pages/__tests__/blTabs.test.tsx docs/
git commit -m "feat(bl): aba Detalhes vira replica do documento B/L"
```

---

## PR 3 — Visão Geral + pipeline de dois trilhos

### Task 3.1: Trilhos como funções puras

**Files:**
- Create: `src/services/blRails.ts`
- Test: `src/services/__tests__/blRails.test.ts`

- [ ] **Step 1: Testes falhando (comportamentos-chave)**

```ts
// src/services/__tests__/blRails.test.ts
import { describe, expect, it } from 'vitest'
import { buildFinancialRail, buildOperationalRail, pickNextAction } from '../blRails'

const baseBl = {
  id: 'BL1', voyage_id: 7, cargo_mode: 'container',
  ce_mercante: null, review_status: 'pending_review',
  customer_reconciliation_status: 'missing_customer', customer_id: null,
  charge_status: 'not_calculated', financial_status: 'pending',
} as never

describe('buildOperationalRail', () => {
  it('sem ATD do POL, saida fica pendente com ETD como detalhe', () => {
    const rail = buildOperationalRail({
      bl: baseBl,
      polSchedule: { etd: '2026-07-01', atd: null },
      podSchedule: { eta: '2026-07-20', ata: null },
      containers: [],
      omission: null,
    })
    expect(rail[0]).toMatchObject({ key: 'pol', state: 'pending' })
  })

  it('com omissao, o estagio do POD vira transbordo', () => {
    const rail = buildOperationalRail({
      bl: baseBl,
      polSchedule: { etd: '2026-07-01', atd: '2026-07-02' },
      podSchedule: null,
      containers: [],
      omission: { omittedPod: 'VITORIA', dischargePod: 'SANTOS' },
    })
    expect(rail[1]).toMatchObject({ key: 'pod', state: 'diverted' })
    expect(rail[1].detail).toMatch(/SANTOS/)
  })

  it('conta descarga e devolucao por container distinto', () => {
    const rail = buildOperationalRail({
      bl: baseBl,
      polSchedule: null, podSchedule: null, omission: null,
      containers: [
        { container_number: 'ABCD1234567', discharge_date: '2026-07-21', return_date: '2026-07-25' },
        { container_number: 'ABCD7654321', discharge_date: '2026-07-21', return_date: null },
      ],
    })
    expect(rail[2].detail).toBe('2/2 descarregados')
    expect(rail[3].detail).toBe('1/2 devolvidos')
    expect(rail[3].state).toBe('pending')
  })
})

describe('buildFinancialRail + pickNextAction', () => {
  it('CE ausente e o primeiro pendente e vira proxima acao', () => {
    const rail = buildFinancialRail({ bl: baseBl, latestInvoice: null, demurrageInvoices: [] })
    expect(rail[0]).toMatchObject({ key: 'ce', state: 'pending' })
    expect(pickNextAction(rail)?.key).toBe('ce')
  })

  it('fatura paga fecha o trilho sem estagio de demurrage quando nao ha invoice de demurrage', () => {
    const rail = buildFinancialRail({
      bl: { ...baseBl, ce_mercante: '123456789012345', review_status: 'reviewed', customer_reconciliation_status: 'reconciled', customer_id: 9, charge_status: 'ready_for_billing', financial_status: 'paid' } as never,
      latestInvoice: { id: 1, status: 'paid', total_brl: 100 } as never,
      demurrageInvoices: [],
    })
    expect(rail.every((s) => s.state === 'done')).toBe(true)
    expect(rail.find((s) => s.key === 'demurrage')).toBeUndefined()
    expect(pickNextAction(rail)).toBeNull()
  })
})
```

- [ ] **Step 2: Implementar**

```ts
// src/services/blRails.ts
import type { BL } from '../types/database'

export type RailState = 'done' | 'pending' | 'blocked' | 'diverted'

export type RailStage = {
  key: string
  label: string
  detail: string
  state: RailState
  /** Link escopado no B/L (aba da propria ficha ou rota externa). */
  href?: string
}

type RailBl = Pick<
  BL,
  'id' | 'voyage_id' | 'cargo_mode' | 'ce_mercante' | 'review_status'
  | 'customer_reconciliation_status' | 'customer_id' | 'charge_status' | 'financial_status'
>

export type RailContainer = { container_number: string; discharge_date: string | null; return_date: string | null }
export type RailSchedule = { etd?: string | null; atd?: string | null; eta?: string | null; ata?: string | null }
export type RailOmission = { omittedPod: string; dischargePod: string }
export type RailInvoice = { id: number; status: string | null; total_brl: number | null }
export type RailDemurrageInvoice = { id: number; status: string | null }

const fmt = (value: string | null | undefined) =>
  value ? new Intl.DateTimeFormat('pt-BR', { timeZone: 'UTC' }).format(new Date(value)) : null

function distinct(containers: RailContainer[], has: (c: RailContainer) => boolean) {
  const all = new Set(containers.map((c) => c.container_number))
  const done = new Set(containers.filter(has).map((c) => c.container_number))
  return { done: done.size, total: all.size }
}

export function buildOperationalRail(input: {
  bl: RailBl
  polSchedule: RailSchedule | null
  podSchedule: RailSchedule | null
  containers: RailContainer[]
  omission: RailOmission | null
}): RailStage[] {
  const { bl, polSchedule, podSchedule, containers, omission } = input
  const voyageHref = bl.voyage_id ? `/viagens/${bl.voyage_id}` : undefined

  const pol: RailStage = polSchedule?.atd
    ? { key: 'pol', label: 'Saida do POL', detail: `ATD ${fmt(polSchedule.atd)}`, state: 'done', href: voyageHref }
    : { key: 'pol', label: 'Saida do POL', detail: polSchedule?.etd ? `ETD ${fmt(polSchedule.etd)}` : 'Sem previsao', state: 'pending', href: voyageHref }

  const pod: RailStage = omission
    ? { key: 'pod', label: 'Chegada ao POD', detail: `Omitida — descarga em ${omission.dischargePod}`, state: 'diverted', href: voyageHref }
    : podSchedule?.ata
      ? { key: 'pod', label: 'Chegada ao POD', detail: `ATA ${fmt(podSchedule.ata)}`, state: 'done', href: voyageHref }
      : { key: 'pod', label: 'Chegada ao POD', detail: podSchedule?.eta ? `ETA ${fmt(podSchedule.eta)}` : 'Sem previsao', state: 'pending', href: voyageHref }

  if (bl.cargo_mode !== 'container') return [pol, pod]

  const discharge = distinct(containers, (c) => Boolean(c.discharge_date))
  const returned = distinct(containers, (c) => Boolean(c.return_date))

  return [
    pol,
    pod,
    {
      key: 'discharge', label: 'Descarga',
      detail: `${discharge.done}/${discharge.total} descarregados`,
      state: discharge.total > 0 && discharge.done === discharge.total ? 'done' : 'pending',
      href: `/containers`,
    },
    {
      key: 'return', label: 'Devolucao',
      detail: `${returned.done}/${returned.total} devolvidos`,
      state: returned.total > 0 && returned.done === returned.total ? 'done' : 'pending',
      href: `/manifestos/${bl.id}?tab=faturamento`,
    },
  ]
}

export function buildFinancialRail(input: {
  bl: RailBl
  latestInvoice: RailInvoice | null
  demurrageInvoices: RailDemurrageInvoice[]
}): RailStage[] {
  const { bl, latestInvoice, demurrageInvoices } = input
  const fichaFat = `/manifestos/${bl.id}?tab=faturamento`
  const fichaDet = `/manifestos/${bl.id}`

  const ce: RailStage = bl.ce_mercante
    ? { key: 'ce', label: 'CE Mercante', detail: bl.ce_mercante, state: 'done' }
    : { key: 'ce', label: 'CE Mercante', detail: 'Cadastrar CE', state: 'pending', href: fichaDet }

  const reviewOk = bl.review_status === 'reviewed' || bl.review_status === 'ok'
  const customerOk = bl.customer_reconciliation_status === 'reconciled' && bl.customer_id != null
  const review: RailStage = reviewOk && customerOk
    ? { key: 'review', label: 'Revisao & Cliente', detail: 'OK', state: 'done' }
    : { key: 'review', label: 'Revisao & Cliente', detail: reviewOk ? 'Vincular cliente' : 'Revisar B/L', state: 'pending', href: reviewOk ? fichaFat : fichaDet }

  const chargesOk = bl.charge_status === 'ready_for_billing' || bl.charge_status === 'exempt'
  const charges: RailStage = chargesOk
    ? { key: 'charges', label: 'Taxas Locais', detail: bl.charge_status === 'exempt' ? 'Isento' : 'Pronto p/ faturar', state: 'done' }
    : { key: 'charges', label: 'Taxas Locais', detail: bl.charge_status === 'not_calculated' || !bl.charge_status ? 'Calcular taxas' : 'Revisar taxas', state: 'pending', href: fichaFat }

  const invoice: RailStage = latestInvoice
    ? { key: 'invoice', label: 'Fatura', detail: `#${latestInvoice.id} ${latestInvoice.status ?? ''}`.trim(), state: latestInvoice.status === 'cancelled' ? 'blocked' : 'done', href: `/faturamento?invoice=${latestInvoice.id}` }
    : { key: 'invoice', label: 'Fatura', detail: 'Nao emitida', state: 'pending', href: fichaFat }

  const payment: RailStage = bl.financial_status === 'paid'
    ? { key: 'payment', label: 'Pagamento', detail: 'Pago', state: 'done' }
    : { key: 'payment', label: 'Pagamento', detail: 'Pendente', state: 'pending', href: '/faturamento' }

  const rail = [ce, review, charges, invoice, payment]

  if (demurrageInvoices.length > 0) {
    const allPaid = demurrageInvoices.every((d) => d.status === 'paid')
    rail.push({
      key: 'demurrage', label: 'Demurrage',
      detail: `${demurrageInvoices.length} invoice(s)${allPaid ? ' pagas' : ''}`,
      state: allPaid ? 'done' : 'pending',
      href: fichaFat,
    })
  }

  return rail
}

export function pickNextAction(financialRail: RailStage[]): RailStage | null {
  return financialRail.find((stage) => stage.state === 'pending' || stage.state === 'blocked') ?? null
}
```

Nota: `bl_containers` hoje não expõe `discharge_date`/`return_date` com esses
nomes em todos os pontos — conferir os nomes reais das colunas em
`src/services/demurrage/demurrageContainers.ts` (datas de descarga/devolução
usadas pelo demurrage) e alinhar `RailContainer` a eles antes de implementar.

- [ ] **Step 3: Rodar e ver passar; commit**

```bash
npx vitest run src/services/__tests__/blRails.test.ts
git add src/services/blRails.ts src/services/__tests__/blRails.test.ts
git commit -m "feat(bl): trilhos operacional e financeiro como funcoes puras"
```

### Task 3.2: Hook `useBlCockpit` + componente do pipeline

**Files:**
- Create: `src/hooks/useBlCockpit.ts`
- Create: `src/components/bl/BlRailsPipeline.tsx`
- Test: `src/components/bl/__tests__/BlRailsPipeline.test.tsx`

- [ ] **Step 1: Hook**

```ts
// src/hooks/useBlCockpit.ts
import { useQuery } from '@tanstack/react-query'
import { queryKeys } from '../services/queryKeys'
import {
  buildVoyagePodEntityId, buildVoyagePolEntityId,
  listVoyagePodSchedules, listVoyagePolSchedules,
} from '../services/voyageRouteSchedules'
import { listVoyageOmissions, listBlTransshipments } from '../services/transshipments'
import type { BLDetail } from '../types/database'

/** Dados transversais da Visao Geral: escalas do POL/POD e omissao/transbordo do B/L. */
export function useBlCockpit(bl: BLDetail | undefined) {
  return useQuery({
    queryKey: ['bl-cockpit', bl?.id ?? 'none'],
    enabled: Boolean(bl?.id && bl?.voyage_id),
    queryFn: async () => {
      const voyageId = Number(bl!.voyage_id)
      const polId = buildVoyagePolEntityId(voyageId, bl!.pol)
      const podId = buildVoyagePodEntityId(voyageId, bl!.pod)
      const [polSchedules, podSchedules, omissions] = await Promise.all([
        listVoyagePolSchedules([polId]),
        listVoyagePodSchedules([podId]),
        listVoyageOmissions(voyageId),
      ])
      // Omissao que afeta este B/L: aquela cujo POD omitido bate com o POD do B/L
      const omission = omissions.find((o) => o.omittedPod === String(bl!.pod ?? '').trim().toUpperCase()) ?? null
      const transshipment = omission
        ? (await listBlTransshipments(omission.id)).find((t) => t.blId === bl!.id) ?? null
        : null
      return {
        polSchedule: polSchedules.get(polId) ?? null,
        podSchedule: podSchedules.get(podId) ?? null,
        omission,
        transshipment,
      }
    },
  })
}
```

Acrescentar em `queryKeys.bls`: `cockpit: (blId?: string) => (blId === undefined ? (['bl-cockpit'] as const) : (['bl-cockpit', blId] as const))`
e usar `queryKeys.bls.cockpit(bl?.id)` no hook.

- [ ] **Step 2: Componente com teste**

```tsx
// src/components/bl/__tests__/BlRailsPipeline.test.tsx
import { render, screen } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import { describe, expect, it } from 'vitest'
import { BlRailsPipeline } from '../BlRailsPipeline'

const stage = (key: string, label: string, state: 'done' | 'pending', detail = 'x') => ({ key, label, state, detail })

describe('BlRailsPipeline', () => {
  it('mostra os dois trilhos e a proxima acao', () => {
    render(
      <MemoryRouter>
        <BlRailsPipeline
          operational={[stage('pol', 'Saida do POL', 'done'), stage('pod', 'Chegada ao POD', 'pending')]}
          financial={[stage('ce', 'CE Mercante', 'pending', 'Cadastrar CE')]}
          nextAction={{ key: 'ce', label: 'CE Mercante', detail: 'Cadastrar CE', state: 'pending', href: '/manifestos/BL1' }}
        />
      </MemoryRouter>,
    )
    expect(screen.getByText('Operacional')).toBeInTheDocument()
    expect(screen.getByText('Financeiro')).toBeInTheDocument()
    expect(screen.getByText(/Proxima acao/i)).toBeInTheDocument()
    expect(screen.getByText('Cadastrar CE', { selector: 'a *' })).toBeInTheDocument()
  })
})
```

```tsx
// src/components/bl/BlRailsPipeline.tsx
import { Link } from 'react-router-dom'
import type { RailStage, RailState } from '../../services/blRails'

const stateDot: Record<RailState, string> = {
  done: 'bg-emerald-500',
  pending: 'bg-amber-400',
  blocked: 'bg-red-500',
  diverted: 'bg-sky-400',
}

function Stage({ stage }: { stage: RailStage }) {
  const inner = (
    <div className="flex min-w-[9rem] flex-col gap-1 rounded-xl border border-[var(--app-border)] bg-[var(--app-surface)] px-3 py-2">
      <div className="flex items-center gap-2">
        <span className={`h-2 w-2 rounded-full ${stateDot[stage.state]}`} aria-hidden="true" />
        <span className="text-xs font-medium uppercase tracking-wider text-[var(--app-muted)]">{stage.label}</span>
      </div>
      <span className="text-sm font-semibold text-[var(--app-text-strong)]">{stage.detail}</span>
    </div>
  )
  return stage.href ? <Link to={stage.href} className="hover:opacity-80">{inner}</Link> : inner
}

function Rail({ title, stages }: { title: string; stages: RailStage[] }) {
  return (
    <div className="grid gap-2">
      <span className="text-xs font-semibold uppercase tracking-wider text-[var(--app-muted)]">{title}</span>
      <div className="flex flex-wrap items-center gap-2">
        {stages.map((stage, index) => (
          <div key={stage.key} className="flex items-center gap-2">
            {index > 0 ? <span className="text-[var(--app-muted)]">→</span> : null}
            <Stage stage={stage} />
          </div>
        ))}
      </div>
    </div>
  )
}

export function BlRailsPipeline({ operational, financial, nextAction }: {
  operational: RailStage[]
  financial: RailStage[]
  nextAction: RailStage | null
}) {
  return (
    <div className="grid gap-4 rounded-2xl border border-[var(--app-border)] bg-[var(--app-surface-muted)] p-4">
      {nextAction ? (
        <Link to={nextAction.href ?? '#'} className="flex items-center gap-2 rounded-xl border border-amber-400/40 bg-amber-400/10 px-3 py-2 hover:opacity-90">
          <span className="text-xs font-semibold uppercase tracking-wider text-amber-300">Proxima acao</span>
          <span className="text-sm font-semibold text-[var(--app-text-strong)]">{nextAction.detail}</span>
        </Link>
      ) : null}
      <Rail title="Operacional" stages={operational} />
      <Rail title="Financeiro" stages={financial} />
    </div>
  )
}
```

- [ ] **Step 3: Rodar e ver passar; commit**

```bash
npx vitest run src/components/bl/__tests__/BlRailsPipeline.test.tsx
git add src/hooks/useBlCockpit.ts src/components/bl/BlRailsPipeline.tsx src/components/bl/__tests__/BlRailsPipeline.test.tsx src/services/queryKeys.ts
git commit -m "feat(bl): hook de cockpit e pipeline de dois trilhos"
```

### Task 3.3: Aba Visão Geral + 4 abas no `BlDetalhe`

**Files:**
- Create: `src/components/bl/BlVisaoGeralTab.tsx`
- Modify: `src/pages/BlDetalhe.tsx`
- Delete: `src/components/shared/BLPipeline.tsx`, `src/services/blStatusService.ts` (usados só pela ficha)
- Test: `src/pages/__tests__/blTabs.test.tsx`

- [ ] **Step 1: Atualizar o teste de abas (falhando)**

```tsx
// blTabs.test.tsx — substituir as asserções de BL_TABS
expect(BL_TABS.map((t) => t.key)).toEqual(['visao-geral', 'detalhes', 'faturamento', 'historico'])
// aba padrão passa a ser visao-geral (sem ?tab); 'detalhes' agora sincroniza ?tab=detalhes
```

- [ ] **Step 2: `BlVisaoGeralTab` (cards de Viagem/Carga/Cliente/Financeiro; Transbordo entra no PR 4, Portal/Baplie no PR 5)**

```tsx
// src/components/bl/BlVisaoGeralTab.tsx
import { Link } from 'react-router-dom'
import { Card } from '../ui/Card'
import { Badge } from '../ui/Badge'
import type { BLDetail } from '../../types/database'
import type { ContainerSummary, BreakbulkSummary } from './BlCargaTab'
import type { useBlCockpit } from '../../hooks/useBlCockpit'

const dt = (value: string | null | undefined) =>
  value ? new Intl.DateTimeFormat('pt-BR', { timeZone: 'UTC' }).format(new Date(value)) : '—'

export function BlVisaoGeralTab({ active, bl, cockpit, isContainerMode, containerSummary, breakbulkSummary }: {
  active: boolean
  bl: BLDetail
  cockpit: ReturnType<typeof useBlCockpit>['data']
  isContainerMode: boolean
  containerSummary: ContainerSummary
  breakbulkSummary: BreakbulkSummary
}) {
  if (!active) return null
  return (
    <div className="grid gap-5 lg:grid-cols-2">
      <Card>
        <h3 className="mb-3 text-sm font-semibold">Viagem &amp; Escala</h3>
        <dl className="grid gap-2 text-sm sm:grid-cols-2">
          <Item label="Armador / Navio / Viagem">
            {bl.voyage_id ? (
              <Link className="font-semibold text-[#58a6ff] hover:underline" to={`/viagens/${bl.voyage_id}`}>
                {[bl.voyage?.vessel?.carrier?.name, bl.voyage?.vessel?.name, bl.voyage?.voyage_number].filter(Boolean).join(' / ') || '—'}
              </Link>
            ) : '—'}
          </Item>
          <Item label="Trecho">{`${bl.pol ?? '—'} → ${bl.pod ?? '—'}`}</Item>
          <Item label="Saida do POL">{cockpit?.polSchedule?.atd ? `ATD ${dt(cockpit.polSchedule.atd)}` : `ETD ${dt(cockpit?.polSchedule?.etd)}`}</Item>
          <Item label="Chegada ao POD">{cockpit?.podSchedule?.ata ? `ATA ${dt(cockpit.podSchedule.ata)}` : `ETA ${dt(cockpit?.podSchedule?.eta)}`}</Item>
        </dl>
      </Card>

      <Card>
        <h3 className="mb-3 text-sm font-semibold">Carga</h3>
        {isContainerMode ? (
          <dl className="grid gap-2 text-sm sm:grid-cols-3">
            <Item label="Containers">{String(containerSummary.distinct)}</Item>
            <Item label="IMO">{String(containerSummary.imo)}</Item>
            <Item label="OOG">{String(containerSummary.oog)}</Item>
          </dl>
        ) : (
          <dl className="grid gap-2 text-sm sm:grid-cols-3">
            <Item label="Maquinas">{String(breakbulkSummary.machines)}</Item>
            <Item label="Packages">{String(breakbulkSummary.packagesTotal)}</Item>
            <Item label="Peso (t)">{String(breakbulkSummary.weightTon)}</Item>
          </dl>
        )}
      </Card>

      <Card>
        <h3 className="mb-3 text-sm font-semibold">Cliente</h3>
        {bl.customer ? (
          <div className="text-sm">
            <div className="font-semibold">{bl.customer.name}</div>
            <div className="text-[var(--app-muted)]">{bl.customer.cnpj_cpf}</div>
          </div>
        ) : (
          <Badge tone="yellow">Sem cliente vinculado</Badge>
        )}
        <Link className="mt-2 inline-block text-sm font-semibold text-[#58a6ff] hover:underline" to={`/manifestos/${bl.id}?tab=faturamento`}>
          Abrir Faturamento →
        </Link>
      </Card>

      <Card>
        <h3 className="mb-3 text-sm font-semibold">Financeiro</h3>
        <dl className="grid gap-2 text-sm sm:grid-cols-2">
          <Item label="Taxas">{bl.charge_status ?? 'not_calculated'}</Item>
          <Item label="Status">{bl.financial_status ?? 'pending'}</Item>
        </dl>
      </Card>
    </div>
  )
}

function Item({ label, children }: { label: string; children: React.ReactNode }) {
  return (
    <div>
      <dt className="text-xs text-[var(--app-muted)]">{label}</dt>
      <dd className="font-medium text-[var(--app-text-strong)]">{children}</dd>
    </div>
  )
}
```

- [ ] **Step 3: Rewire `BlDetalhe.tsx`**

- `BL_TABS = [{ key: 'visao-geral', label: 'Visao Geral' }, { key: 'detalhes', label: 'Detalhes do B/L' }, { key: 'faturamento', label: 'Faturamento' }, { key: 'historico', label: 'Historico' }]`;
  `isBlTab` aceita os 4; aba padrão (sem `?tab`) = `visao-geral`; `detalhes`
  passa a gravar `?tab=detalhes`.
- Buscar cockpit e trilhos:

```tsx
const cockpitQuery = useBlCockpit(bl)
const { data: demurrageInvoices } = useQuery({
  queryKey: ['demurrage-invoices', { blId: bl?.id }],
  enabled: Boolean(bl?.id),
  queryFn: () => listDemurrageInvoices({ blId: bl!.id }),
})
const operational = buildOperationalRail({ bl, polSchedule: cockpitQuery.data?.polSchedule ?? null, podSchedule: cockpitQuery.data?.podSchedule ?? null, containers: railContainers, omission: cockpitQuery.data?.omission ?? null })
const financial = buildFinancialRail({ bl, latestInvoice, demurrageInvoices: demurrageInvoices ?? [] })
```

  (`latestInvoice` via `useInvoiceLinks([bl.id])` como `BlFaturamentoTab` já
  faz; `railContainers` mapeado de `bl.bl_containers` com os nomes de coluna de
  descarga/devolução usados pelo demurrage.)
- Substituir `<BLPipeline bl={bl} />` por
  `<BlRailsPipeline operational={operational} financial={financial} nextAction={pickNextAction(financial)} />`.
- Renderizar `<BlVisaoGeralTab active={activeTab === 'visao-geral'} … />`.
- Remover import de `BLPipeline`; deletar `BLPipeline.tsx` e
  `blStatusService.ts` (uso único confirmado) e qualquer teste deles.

- [ ] **Step 4: Rodar suíte, docs e abrir PR**

- `docs/modules/manifesto-edi.md` §`/manifestos/:blId`: 4 abas, aba padrão
  `visao-geral`, pipeline de trilhos.
- `docs/RASTREABILIDADE.md`: linha da rota atualizada (componentes novos).
- `CONTEXT.md`: adicionar termo **Visão Geral do B/L** (ver PR 4 se preferir
  concentrar as edições de glossário — não duplicar).

Run: `npm run docs:check && npm run lint && npm test && npm run build`

```bash
git add src/ docs/
git commit -m "feat(bl): aba Visao Geral e pipeline de dois trilhos com proxima acao"
```

---

## PR 4 — Transbordo/COD na ficha do B/L (e remoção na Viagem)

### Task 4.1: Card de Transbordo/COD na Visão Geral

**Files:**
- Create: `src/components/bl/BlTransshipmentCard.tsx`
- Modify: `src/components/bl/BlVisaoGeralTab.tsx` (renderizar o card condicional)
- Modify: `src/hooks/useTransshipments.ts` (invalidations do `useSetBlDisposition` passam a incluir `['bl-cockpit']` e `['bl-detail']`)
- Test: `src/components/bl/__tests__/BlTransshipmentCard.test.tsx`

- [ ] **Step 1: Teste falhando**

```tsx
import { render, screen } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import { describe, expect, it, vi } from 'vitest'
import { BlTransshipmentCard } from '../BlTransshipmentCard'

const omission = { id: 9, voyageId: 7, omittedPod: 'VITORIA', dischargePod: 'SANTOS', reason: null, onwardVesselName: 'MSC X', onwardCarrier: 'MSC', onwardVoyageNumber: '123A', onwardEtd: '2026-07-10', onwardEta: '2026-07-14' }

describe('BlTransshipmentCard', () => {
  it('exibe dados herdados e a acao de COD quando disposicao e transbordo', () => {
    render(
      <MemoryRouter>
        <BlTransshipmentCard omission={omission} disposition="transshipment" saving={false} onCod={vi.fn()} onRestore={vi.fn()} />
      </MemoryRouter>,
    )
    expect(screen.getByText(/SANTOS/)).toBeInTheDocument()
    expect(screen.getByText('MSC X')).toBeInTheDocument()
    expect(screen.getByRole('button', { name: /Marcar COD/ })).toBeInTheDocument()
  })

  it('exibe reversao quando disposicao e cod', () => {
    render(
      <MemoryRouter>
        <BlTransshipmentCard omission={omission} disposition="cod" saving={false} onCod={vi.fn()} onRestore={vi.fn()} />
      </MemoryRouter>,
    )
    expect(screen.getByText(/COD SANTOS/)).toBeInTheDocument()
    expect(screen.getByRole('button', { name: /Reverter para transbordo/ })).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Implementar**

Portar a UI de `BlRow` de `src/components/voyages/TransshipmentPanel.tsx` para
componente próprio da ficha (dados herdados + badge de disposição + botões
Marcar COD / Reverter), acrescentando link "Registro global da omissao →
`/viagens/${omission.voyageId}`". Props puras (`omission`, `disposition`,
`saving`, `onCod`, `onRestore`) para testabilidade; quem conecta é a
`BlVisaoGeralTab` usando `cockpit.omission`/`cockpit.transshipment` e
`useSetBlDisposition(bl.voyage_id)`:

```tsx
{cockpit?.omission && cockpit?.transshipment ? (
  <BlTransshipmentCard
    omission={cockpit.omission}
    disposition={cockpit.transshipment.disposition}
    saving={setTransshipment.isPending || setCod.isPending}
    onCod={() => user?.id && setCod.mutate({ blId: bl.id, omissionId: cockpit.omission!.id, changedBy: user.id })}
    onRestore={() => user?.id && setTransshipment.mutate({ blId: bl.id, omissionId: cockpit.omission!.id, changedBy: user.id })}
  />
) : null}
```

- [ ] **Step 3: Rodar e ver passar; commit**

```bash
npx vitest run src/components/bl/__tests__/BlTransshipmentCard.test.tsx
git add src/components/bl/ src/hooks/useTransshipments.ts
git commit -m "feat(bl): card de transbordo com acao de COD na ficha do B/L"
```

### Task 4.2: Viagem perde a ação — vira leitura com link

**Files:**
- Modify: `src/components/voyages/TransshipmentPanel.tsx`
- Test: teste existente do painel (se houver) ou smoke em `Viagens.behavior.test.tsx`

- [ ] **Step 1: Remover os botões `Marcar COD`/`Reverter` e o uso de `useSetBlDisposition`**

`BlRow` mantém badge de disposição e dados herdados; o B/L vira link:

```tsx
<Link className="font-mono text-sm font-semibold text-[#58a6ff] hover:underline" to={`/manifestos/${transshipment.blId}`}>
  {transshipment.blId}
</Link>
<p className="text-xs text-[var(--app-muted)]">A disposicao (transbordo/COD) e operada na ficha do B/L.</p>
```

- [ ] **Step 2: Glossário e docs**

- `CONTEXT.md` — **Transbordo**: acrescentar "A disposicao individual
  (`transshipment`/`cod`) e operada na ficha do B/L; a Viagem edita apenas o
  registro global e lista os B/Ls afetados para consulta."
- `CONTEXT.md` — novo termo **Visão Geral do B/L**: "Aba padrao da ficha do
  B/L que consolida, para consulta, viagem e escalas, transbordo/COD, carga,
  cliente, portal e financeiro, com o pipeline de trilhos operacional e
  financeiro e a proxima acao."
- `docs/modules/manifesto-edi.md` e módulo de Viagens (se houver): mover a
  descrição da ação de disposição para a ficha do B/L.
- `docs/RASTREABILIDADE.md`: linha nova "B/L — disposição transbordo/COD"
  (origem `BlTransshipmentCard`, RPCs `set_bl_cod`/`set_bl_transshipment`);
  atualizar a linha equivalente de Viagens.

- [ ] **Step 3: Verificação + PR**

Run: `npm run docs:check && npm run lint && npm test && npm run build`

```bash
git add src/components/voyages/TransshipmentPanel.tsx docs/ CONTEXT.md
git commit -m "refactor(viagem): disposicao de transbordo/COD passa a ser operada na ficha do B/L"
```

---

## PR 5 — Portal e Baplie na Visão Geral

### Task 5.1: Migração `206_portal_notifications_bl_id.sql`

**Files:**
- Create: `supabase/migrations/206_portal_notifications_bl_id.sql`
- Test: `src/services/__tests__/portalNotificationsBlIdMigration.test.ts`

- [ ] **Step 1: Teste falhando**

```ts
import { readFileSync } from 'node:fs'
import { describe, expect, it } from 'vitest'

const sql = readFileSync('supabase/migrations/206_portal_notifications_bl_id.sql', 'utf8')

describe('206_portal_notifications_bl_id', () => {
  it('adiciona bl_id opcional com FK', () => {
    expect(sql).toMatch(/ADD COLUMN IF NOT EXISTS bl_id TEXT/)
    expect(sql).toMatch(/REFERENCES public\.bls\(id\) ON DELETE SET NULL/)
  })
  it('reaplica omit_voyage_escala e set_bl_cod preenchendo bl_id', () => {
    for (const fn of ['omit_voyage_escala', 'set_bl_cod']) {
      const body = sql.match(new RegExp(`FUNCTION public\\.${fn}[\\s\\S]*?\\$function\\$;`, 'i'))?.[0] ?? ''
      expect(body).toMatch(/INSERT INTO public\.portal_notifications\s*\([^)]*bl_id/i)
    }
  })
})
```

- [ ] **Step 2: Escrever a migração**

```sql
-- 206_portal_notifications_bl_id.sql
-- Vincula notificacoes do Portal ao B/L de origem (forward-only).
ALTER TABLE public.portal_notifications ADD COLUMN IF NOT EXISTS bl_id TEXT
  REFERENCES public.bls(id) ON DELETE SET NULL;
CREATE INDEX IF NOT EXISTS idx_portal_notifications_bl
  ON public.portal_notifications (bl_id, created_at DESC);

-- Reaplicar (copia integral das versoes vigentes — 201/202 para omissao/COD e
-- 116+sucessoras para o trigger de demurrage_invoices) alterando SOMENTE os
-- INSERT INTO public.portal_notifications para incluir a coluna bl_id com o
-- B/L de origem (b.id / p_bl_id / NEW.bl_id). Notificacoes sem B/L unico
-- (ex.: consolidada) permanecem com bl_id NULL.
```

- [ ] **Step 3: Rodar teste (PASS), aplicar migração via MCP, commit**

```bash
npx vitest run src/services/__tests__/portalNotificationsBlIdMigration.test.ts
git add supabase/migrations/206_portal_notifications_bl_id.sql src/services/__tests__/portalNotificationsBlIdMigration.test.ts
git commit -m "feat(portal): notificacoes vinculadas ao B/L de origem"
```

### Task 5.2: Serviço de status do Portal por B/L

**Files:**
- Create: `src/services/blPortalStatus.ts`
- Test: `src/services/__tests__/blPortalStatus.test.ts`

- [ ] **Step 1: Teste falhando (lógica pura de visibilidade)**

```ts
import { describe, expect, it } from 'vitest'
import { computeBlPortalVisibility } from '../blPortalStatus'

describe('computeBlPortalVisibility', () => {
  it('visivel quando ha CE, cliente e conta ativa', () => {
    expect(computeBlPortalVisibility({ ceMercante: '123456789012345', customerId: 1, accountSituation: 'ativo' }))
      .toEqual({ visible: true, reasons: [] })
  })
  it('lista todos os motivos do bloqueio', () => {
    expect(computeBlPortalVisibility({ ceMercante: null, customerId: null, accountSituation: null }).reasons)
      .toEqual(['Sem CE Mercante', 'Sem cliente vinculado', 'Cliente sem Conta de Portal ativa'])
  })
  it('conta suspensa bloqueia', () => {
    expect(computeBlPortalVisibility({ ceMercante: 'x', customerId: 1, accountSituation: 'suspenso' }).visible).toBe(false)
  })
})
```

- [ ] **Step 2: Implementar**

```ts
// src/services/blPortalStatus.ts
import { supabase } from './supabase'

export type BlPortalVisibility = { visible: boolean; reasons: string[] }

export function computeBlPortalVisibility(input: {
  ceMercante: string | null
  customerId: number | null
  accountSituation: string | null
}): BlPortalVisibility {
  const reasons: string[] = []
  if (!input.ceMercante) reasons.push('Sem CE Mercante')
  if (input.customerId == null) reasons.push('Sem cliente vinculado')
  if (input.customerId != null || input.accountSituation != null) {
    if (input.accountSituation !== 'ativo') reasons.push('Cliente sem Conta de Portal ativa')
  } else {
    reasons.push('Cliente sem Conta de Portal ativa')
  }
  return { visible: reasons.length === 0, reasons }
}

export type BlPortalNotification = {
  id: number
  type: string
  title: string
  created_at: string
  read_at: string | null
}

// ponytail: database.ts nao tipa portal_notifications/customer_portal_accounts;
// casts locais ate regenerar tipos com autorizacao.
export async function getBlPortalStatus(input: { blId: string; ceMercante: string | null; customerId: number | null }) {
  const [accountRes, notificationsRes, disputesRes] = await Promise.all([
    input.customerId == null
      ? Promise.resolve({ data: null, error: null })
      : (supabase.from as unknown as (t: string) => {
          select: (c: string) => { eq: (k: string, v: number) => { maybeSingle: () => Promise<{ data: { account_situation: string } | null; error: Error | null }> } }
        })('customer_portal_accounts').select('account_situation').eq('customer_id', input.customerId).maybeSingle(),
    (supabase.from as unknown as (t: string) => {
      select: (c: string) => { eq: (k: string, v: string) => { order: (k: string, o: { ascending: boolean }) => { limit: (n: number) => Promise<{ data: unknown[] | null; error: Error | null }> } } }
    })('portal_notifications').select('id, type, title, created_at, read_at').eq('bl_id', input.blId).order('created_at', { ascending: false }).limit(10),
    supabase.from('demurrage_invoices').select('id, doc_number, dispute_open, dispute_status').eq('bl_id', input.blId).eq('dispute_open', true),
  ])
  if (accountRes.error) throw accountRes.error
  if (notificationsRes.error) throw notificationsRes.error
  if (disputesRes.error) throw disputesRes.error

  return {
    visibility: computeBlPortalVisibility({
      ceMercante: input.ceMercante,
      customerId: input.customerId,
      accountSituation: accountRes.data?.account_situation ?? null,
    }),
    notifications: (notificationsRes.data ?? []) as BlPortalNotification[],
    openDisputes: (disputesRes.data ?? []) as Array<{ id: number; doc_number: string | null; dispute_status: string | null }>,
  }
}
```

(Conferir nomes reais das colunas de disputa em `demurrage_invoices` — Task 5.2
usa `dispute_open`/`dispute_status`/`doc_number` conforme migração 116; ajustar
se o serviço `demurrageInvoices.ts` mostrar outros.)

- [ ] **Step 3: Rodar e ver passar; commit**

```bash
npx vitest run src/services/__tests__/blPortalStatus.test.ts
git add src/services/blPortalStatus.ts src/services/__tests__/blPortalStatus.test.ts
git commit -m "feat(bl): status de portal por B/L (visibilidade, notificacoes, disputas)"
```

### Task 5.3: Cards Portal e Baplie na Visão Geral

**Files:**
- Create: `src/components/bl/BlPortalCard.tsx`
- Modify: `src/components/bl/BlVisaoGeralTab.tsx`
- Modify: `src/pages/BlDetalhe.tsx` (queries)
- Test: `src/components/bl/__tests__/BlPortalCard.test.tsx`

- [ ] **Step 1: Teste + componente do card de Portal**

```tsx
// __tests__/BlPortalCard.test.tsx
import { render, screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import { BlPortalCard } from '../BlPortalCard'

describe('BlPortalCard', () => {
  it('mostra motivos quando invisivel', () => {
    render(<BlPortalCard status={{ visibility: { visible: false, reasons: ['Sem CE Mercante'] }, notifications: [], openDisputes: [] }} />)
    expect(screen.getByText(/Nao visivel no Portal/)).toBeInTheDocument()
    expect(screen.getByText('Sem CE Mercante')).toBeInTheDocument()
  })
  it('mostra visivel, notificacoes e disputas abertas', () => {
    render(<BlPortalCard status={{
      visibility: { visible: true, reasons: [] },
      notifications: [{ id: 1, type: 'transshipment', title: 'Escala omitida', created_at: '2026-07-10T00:00:00Z', read_at: null }],
      openDisputes: [{ id: 5, doc_number: 'DEM-5', dispute_status: 'aberto' }],
    }} />)
    expect(screen.getByText(/Visivel no Portal/)).toBeInTheDocument()
    expect(screen.getByText('Escala omitida')).toBeInTheDocument()
    expect(screen.getByText(/DEM-5/)).toBeInTheDocument()
  })
})
```

Componente: `Card` com badge verde "Visivel no Portal" ou amarela "Nao visivel
no Portal" + lista de motivos; lista das últimas notificações (título + data +
"lida/não lida"); disputas abertas com `doc_number` e status. Sem ações.

- [ ] **Step 2: Card Baplie (inline na `BlVisaoGeralTab`, sem componente novo)**

No `BlDetalhe.tsx`:

```tsx
const { data: reconciliation } = useVoyageReconciliation(isContainerMode ? bl?.voyage_id : null)
const blDivergences = useMemo(() => {
  if (!reconciliation || !bl) return []
  const numbers = new Set((bl.bl_containers ?? []).map((c) => c.container_number))
  return reconciliation.items.filter((item) =>
    item.kind === 'missing_in_baplie' ? item.bl_number === bl.id : item.baplie_bl_ref === bl.id || numbers.has(item.container_number),
  )
}, [reconciliation, bl])
```

No card Carga da Visão Geral:

```tsx
{isContainerMode ? (
  blDivergences.length > 0 ? (
    <Link to={`/baplie?voyage=${bl.voyage_id}`} className="mt-2 inline-block">
      <Badge tone="yellow">{blDivergences.length} divergencia(s) Baplie</Badge>
    </Link>
  ) : (
    <Badge tone="green">Baplie sem divergencias</Badge>
  )
) : null}
```

E a query do Portal (React Query, chave `['bl-portal-status', bl.id]`):

```tsx
const { data: portalStatus } = useQuery({
  queryKey: ['bl-portal-status', bl?.id],
  enabled: Boolean(bl?.id),
  queryFn: () => getBlPortalStatus({ blId: bl!.id, ceMercante: bl!.ce_mercante, customerId: bl!.customer_id }),
})
```

`<BlPortalCard status={portalStatus} />` renderizado na Visão Geral (skeleton
enquanto `undefined`).

- [ ] **Step 3: Docs, verificação, encerramento do plano**

- `docs/modules/manifesto-edi.md` + `docs/RASTREABILIDADE.md`: blocos Portal e
  Baplie da Visão Geral (leituras novas: `customer_portal_accounts`,
  `portal_notifications.bl_id`, reuso de `baplie-reconciliation`).
- `docs/CHANGELOG.md`: registrar a entrega do cockpit.
- **Encerrar o ciclo de vida:** `git mv docs/plans/2026-07-18-bl-cockpit-360.md docs/archive/plans/`;
  `git mv docs/spec/2026-07-18-bl-cockpit-design.md docs/archive/specs/`;
  remover a linha de `docs/plans/README.md`; atualizar `docs/spec/README.md`.

Run: `npm run docs:check && npm run lint && npm test && npm run build`

- [ ] **Step 4: Commit + PR final**

```bash
git add src/ docs/ supabase/
git commit -m "feat(bl): cards de Portal e Baplie completam a Visao Geral"
```

---

## Riscos e notas

- **`database.ts` protegido:** todos os campos/tabelas novos usam tipos locais
  com `ponytail:` até regeneração autorizada dos tipos (padrão já existente).
- **Nomes de colunas de descarga/devolução** em `bl_containers`: confirmar em
  `demurrageContainers.ts` antes do PR 3 (o trilho operacional depende deles).
- **RLS:** as leituras novas do PR 5 (`customer_portal_accounts`,
  `portal_notifications`) são feitas pela sessão interna; conferir policies de
  leitura interna antes de implementar — se a RLS bloquear, expor RPC
  `security definer` de leitura mínima em migração adicional.
- **Forward-only:** campos documentais e `bl_id` de notificações ficam NULL no
  histórico; a UI mostra "—" sem tratamento especial.
- **Aba padrão muda** de `detalhes` para `visao-geral`: links externos com
  `?tab=detalhes` continuam funcionando (chave nova sincronizada); links sem
  `tab` passam a abrir o cockpit — comportamento desejado.
