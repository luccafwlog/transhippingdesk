# Implementation Plans — Security Audit 2026-07-07

Generated by the improve skill (deep, security-only audit) on 2026-07-07 at
commit `a894c5d`. Execute in the order below unless dependencies say
otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done.

These plans are **advisor output**: findings were produced by parallel
read-only audits (database/RLS, edge functions, frontend, CI/config) and every
cited excerpt was re-verified against the code by the advisor before writing.
This index covers only this audit; the dated remediation plans directly under
`docs/plans/` belong to other efforts.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Restrict `voyage_route_ce_master` RLS to active internal users | P1 | S | — | TODO |
| 002 | Raise the auth password floor and pin the signup posture | P1 | S | — | TODO |
| 003 | Harden Edge Function auth secrets, error responses, config declarations | P2 | S–M | — | TODO |
| 004 | Add a Sentry `beforeSend` scrubber (CNPJ/CPF/email) | P2 | S | — | TODO |
| 005 | Harden GitHub Actions (least-privilege token, pinned firebase-tools) | P2 | S | — | TODO |
| 006 | Extend the upload guard with a file-type allowlist | P3 | S–M | — | TODO |
| 007 | Close the `.gitignore` blind spot for generated env files | P3 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- No hard dependencies. 001 should land first because it closes the only
  directly reachable exposure (Portal-reachable table); 002 second because an
  open-signup posture would amplify any future policy gap of 001's class.
- 001 and 002 both touch Supabase posture but different files — safe in
  parallel. 003's operational cutover (webhook/cron secret headers) is a
  dashboard action tracked inside that plan.
- Plans 002, 003, 005, 007 each end with an **operator checklist** for
  dashboard/settings actions the repo cannot make — the plan is not fully
  closed until its checklist is confirmed.

## Audit summary (context for reviewers)

Posture found to be strong overall: RLS default-deny discipline,
`search_path` pinning, anon revocation (ADR 0011), `portal_resolve_login`
throttling (ADR 0013), edge-function caller auth, CSV formula-injection
neutralization, CSP headers, and secret hygiene all verified. `npm audit`:
0 vulnerabilities (prod and dev). The one real access-control defect found is
plan 001 (a regression of the class fixed in migration 160).

## Findings considered and rejected (do not re-audit)

- **Legacy portal token RPCs still granted to `anon`** — dropped in migration
  114; blanket revoke in 152 covers drift; remaining stale variants fail
  closed at runtime (call a dropped function). Dead-code cleanup only, no
  security exposure.
- **Anon key committed in `.codex/environments/environment.toml` as a leak**
  — the anon key is public by design (ships in the SPA bundle); handled as
  hygiene in plan 007, not as a credential leak. No history rewrite.
- **`https_proxy`-style / by-design platform conventions** — dual Supabase
  sessions, client-side checks as UX-only, public Sentry DSN
  (`src/lib/telemetry.ts:6-9`): all documented decisions (ADR 0004, audit
  T10), not findings.
- **SHA-pinning first-party GitHub actions (`actions/checkout@v4` etc.)** —
  considered, deferred: low exploitability, high churn; only
  `firebase-tools@latest` (third-party, runs with deploy credentials)
  warranted pinning (plan 005).
- **Edge-function CORS** — `provision-portal-user` allowlist is fail-closed;
  `notify-invoice-issued` reflects only the project origin and uses no
  credentials. Verified correct, no change.
- **`echo '${{ secrets.FIREBASE_… }}' > /tmp/firebase-sa.json`** in the deploy
  workflow — standard pattern; GitHub masks the secret in logs. Not a finding.
